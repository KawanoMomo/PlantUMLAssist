# Activity v1.0.2 Implementation Plan (Branch-aware insertion)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Activity の `resolveInsertLine` を X 座標も考慮するように変更し、 if/fork の水平分岐 composite で視覚と挿入結果が一致するようにする + hover ガイドを resolved rect のカラム幅に制限する。

**Architecture:** `resolveInsertLine` の signature を `(overlayEl, x, y)` に変更、 ユークリッド距離で最近接 rect を選ぶ。 戻り値に `rectX` / `rectWidth` を追加し、 hover ガイドの span 制御に流用。 Sequence の `resolveInsertLine` も signature 揃え (X は無視)、 caller (`src/app.js`) を 1 箇所改修。

**Tech Stack:** ES5 JavaScript (no transpile)、jsdom + custom tests/run-tests.js (Jest-like API: `toBe` / `toContain` / `toBeDefined` / `toBeNull` / `toBeGreaterThan` / `toBeLessThan` / `toThrow` / `not.toContain` / `not.toBe` のみ — `toMatch` / `toBeTruthy` 不可)、Playwright E2E、PlantUML render via plantuml.jar or online。

**Spec:** `docs/superpowers/specs/2026-04-29-activity-v1.0.2-design.md`

**Branch:** `feat/activity-v1.0.2` (parent: `feat/activity-v1.0.1`)

---

## File Structure

| File | 責務 | 状態 |
|---|---|---|
| `src/modules/activity.js` | `resolveInsertLine` を X-aware euclidean に置換、 戻り値に `rectX`/`rectWidth` 追加 | **modify** |
| `src/ui/sequence-overlay.js` | `resolveInsertLine(overlayEl, y)` → `(overlayEl, x, y)` に signature 揃え (X は無視) | **modify** |
| `src/app.js` | mousemove/click handler で X も計算して `resolveInsertLine` に渡す、 `drawHoverGuide` を `(y, rectX, rectWidth)` 受け取る形に拡張 | **modify** |
| `tests/activity-updater.test.js` | 既存 `resolveInsertLine` 4 テストを `(overlay, x, y)` 形式に更新 + branch disambiguation テスト 4 個追加 | **modify** |
| `tests/sequence-overlay.test.js` | 既存 `resolveInsertLine` テストを `(overlay, x, y)` 形式に更新 (X は任意の値で OK) | **modify** |
| `tests/e2e/activity.spec.js` | branch-aware insertion E2E + hover ガイド span 検証を追加 | **modify** |
| `CHANGELOG.md` | v1.0.2 セクション追加 | **modify** |

---

## Task 1: Activity `resolveInsertLine` を X-aware euclidean に置換

**目的:** ユークリッド距離で最近接 rect を選ぶアルゴリズムに置換、戻り値に `rectX` / `rectWidth` 追加。

**Files:**
- Modify: `src/modules/activity.js` の `function resolveInsertLine(overlayEl, y) { ... }` 全体
- Test: `tests/activity-updater.test.js` の `describe('activity resolveInsertLine ...')` 既存ブロック更新 + 拡張

### Step 1: 既存テストを新 signature に更新 + 新規テスト追加

`tests/activity-updater.test.js` の既存 `describe('activity resolveInsertLine (Y → line/position mapping)' ...)` ブロックを以下で全置換:

```javascript
describe('activity resolveInsertLine (X+Y → line/position mapping)', function() {
  function makeOverlay(rectAttrs) {
    var ov = document.createElement('div');
    rectAttrs.forEach(function(attrs) {
      var r = document.createElementNS('http://www.w3.org/2000/svg', 'rect');
      Object.keys(attrs).forEach(function(k) { r.setAttribute(k, attrs[k]); });
      r.setAttribute('width', attrs.width || '100');
      ov.appendChild(r);
    });
    return ov;
  }
  test('returns null for empty overlay', function() {
    var ov = makeOverlay([]);
    expect(actMod.resolveInsertLine(ov, 50, 50)).toBe(null);
  });
  test('y above all rects: nearest by Y, position before', function() {
    var ov = makeOverlay([
      { 'data-type': 'action', 'data-line': '5', x: '0', y: '50', height: '20', width: '100' },
      { 'data-type': 'action', 'data-line': '7', x: '0', y: '100', height: '20', width: '100' },
    ]);
    var res = actMod.resolveInsertLine(ov, 50, 10);
    expect(res.line).toBe(5);
    expect(res.position).toBe('before');
  });
  test('y between two rects: nearest, after upper', function() {
    var ov = makeOverlay([
      { 'data-type': 'action', 'data-line': '5', x: '0', y: '50', height: '20', width: '100' },
      { 'data-type': 'action', 'data-line': '7', x: '0', y: '100', height: '20', width: '100' },
    ]);
    var res = actMod.resolveInsertLine(ov, 50, 80);
    expect(res.line).toBe(5);
    expect(res.position).toBe('after');
  });
  test('y below all rects: nearest by Y, after last', function() {
    var ov = makeOverlay([
      { 'data-type': 'action', 'data-line': '5', x: '0', y: '50', height: '20', width: '100' },
      { 'data-type': 'action', 'data-line': '7', x: '0', y: '100', height: '20', width: '100' },
    ]);
    var res = actMod.resolveInsertLine(ov, 50, 200);
    expect(res.line).toBe(7);
    expect(res.position).toBe('after');
  });
  test('mixed kinds (start/action/decision/stop) all considered', function() {
    var ov = makeOverlay([
      { 'data-type': 'start', 'data-line': '2', x: '0', y: '20', height: '20', width: '100' },
      { 'data-type': 'decision', 'data-line': '4', x: '0', y: '60', height: '24', width: '100' },
      { 'data-type': 'action', 'data-line': '5', x: '0', y: '100', height: '20', width: '100' },
      { 'data-type': 'stop', 'data-line': '7', x: '0', y: '150', height: '22', width: '100' },
    ]);
    var res = actMod.resolveInsertLine(ov, 50, 80);
    expect(res.line).toBe(4);
    expect(res.position).toBe('after');
  });
  // === Branch disambiguation tests (v1.0.2) ===
  test('horizontal branches at same Y: closer X wins (then side)', function() {
    var ov = makeOverlay([
      { 'data-type': 'action', 'data-line': '3', x: '0', y: '100', height: '20', width: '60' },   // then column
      { 'data-type': 'action', 'data-line': '5', x: '150', y: '100', height: '20', width: '60' }, // else column
    ]);
    var res = actMod.resolveInsertLine(ov, 30, 110);  // click in then column
    expect(res.line).toBe(3);
  });
  test('horizontal branches at same Y: closer X wins (else side)', function() {
    var ov = makeOverlay([
      { 'data-type': 'action', 'data-line': '3', x: '0', y: '100', height: '20', width: '60' },
      { 'data-type': 'action', 'data-line': '5', x: '150', y: '100', height: '20', width: '60' },
    ]);
    var res = actMod.resolveInsertLine(ov, 180, 110);  // click in else column
    expect(res.line).toBe(5);
  });
  test('Y is dominant when X is similar (top-level vertical flow)', function() {
    var ov = makeOverlay([
      { 'data-type': 'action', 'data-line': '3', x: '0', y: '50', height: '20', width: '100' },
      { 'data-type': 'action', 'data-line': '5', x: '0', y: '150', height: '20', width: '100' },
    ]);
    var res = actMod.resolveInsertLine(ov, 50, 60);  // closer to first rect
    expect(res.line).toBe(3);
  });
  test('returns rectX and rectWidth for hover guide', function() {
    var ov = makeOverlay([
      { 'data-type': 'action', 'data-line': '3', x: '20', y: '50', height: '20', width: '60' },
    ]);
    var res = actMod.resolveInsertLine(ov, 50, 60);
    expect(res.rectX).toBe(20);
    expect(res.rectWidth).toBe(60);
  });
});
```

### Step 2: Run tests, verify FAIL

Run: `node tests/run-tests.js`
Expected: 4 existing tests pass (function called with 3 args is forward-compatible, but new assertions on rectX/rectWidth fail) + 4 new tests fail (branch disambiguation logic not implemented yet).

Actually expected: existing 4 tests fail because they call `resolveInsertLine(ov, 50)` style (Y only) but new tests call `(ov, x, y)`. The current impl ignores extra args. So:
- existing 4 tests with new signature (X, Y both passed): some may pass coincidentally, some fail
- new 4 tests: fail because branch-aware logic not present

Note: implementer should run and observe, then proceed.

### Step 3: Replace `resolveInsertLine` implementation

Locate `function resolveInsertLine(overlayEl, y) { ... }` in `src/modules/activity.js` (use Grep — line ~738 in v1.0.0 era, but shifted considerably after v1.0.1 additions). Replace the entire function with:

```javascript
  // Map a click/hover position (in SVG/overlay coordinates) to the nearest
  // rect by Euclidean distance, then determine before/after by Y relative to
  // the rect center. X is honored for branch disambiguation in if/fork composites.
  function resolveInsertLine(overlayEl, x, y) {
    if (!overlayEl) return null;
    var rects = overlayEl.querySelectorAll(
      'rect[data-type="action"], rect[data-type="decision"], rect[data-type="start"],' +
      'rect[data-type="stop"], rect[data-type="end"], rect[data-type="fork"]'
    );
    if (rects.length === 0) return null;
    var best = null;
    var bestDist = Infinity;
    Array.prototype.forEach.call(rects, function(r) {
      var rx = parseFloat(r.getAttribute('x'));
      var ry = parseFloat(r.getAttribute('y'));
      var rw = parseFloat(r.getAttribute('width'));
      var rh = parseFloat(r.getAttribute('height'));
      var cx = rx + rw / 2;
      var cy = ry + rh / 2;
      var dx = x - cx;
      var dy = y - cy;
      var d = Math.sqrt(dx * dx + dy * dy);
      if (d < bestDist) {
        bestDist = d;
        best = {
          line: parseInt(r.getAttribute('data-line'), 10),
          cy: cy,
          rx: rx,
          rw: rw,
        };
      }
    });
    if (!best) return null;
    return {
      line: best.line,
      position: y < best.cy ? 'before' : 'after',
      rectX: best.rx,
      rectWidth: best.rw,
    };
  }
```

### Step 4: Run tests, verify PASS

Run: `node tests/run-tests.js`
Expected: 8 tests in `activity resolveInsertLine` describe pass (4 updated + 4 new). Total count should increase by ~4 from baseline (existing 4 were renamed/updated, 4 new tests).

### Step 5: Commit

```bash
git add src/modules/activity.js tests/activity-updater.test.js
git commit -m "$(cat <<'EOF'
feat(activity): X-aware resolveInsertLine with euclidean distance

if/fork の水平分岐 composite でクリック位置と挿入先 branch が
一致するよう、 X 座標も考慮した最近接 rect 選択に変更。
戻り値に rectX/rectWidth を追加し、 hover ガイドの span 制御に流用。

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 2: Sequence `resolveInsertLine` の signature 揃え

**目的:** caller (`src/app.js`) で kind 分岐なく `resolveInsertLine(overlayEl, x, y)` を呼べるよう、 Sequence 側も同 signature に揃える。 X は本体内で無視。

**Files:**
- Modify: `src/ui/sequence-overlay.js:248-264` (`resolveInsertLine` 関数)
- Test: `tests/sequence-overlay.test.js` の `resolveInsertLine` テスト

### Step 1: Find existing Sequence resolveInsertLine tests

Grep tests/sequence-overlay.test.js for `resolveInsertLine` and inspect existing test signatures.

```bash
grep -n "resolveInsertLine" tests/sequence-overlay.test.js
```

Existing tests likely call `resolveInsertLine(ov, y)`. They need to be updated to `(ov, x, y)` (X can be any value, e.g., 0).

### Step 2: Update existing Sequence tests

In `tests/sequence-overlay.test.js`, find each call to `resolveInsertLine(ov, ...)` and add X = 0 as the second argument:

Before:
```javascript
var res = seqMod.resolveInsertLine(ov, 50);
```

After:
```javascript
var res = seqMod.resolveInsertLine(ov, 0, 50);
```

Apply this transformation to all `resolveInsertLine` invocations in the test file.

### Step 3: Run tests, verify FAIL

Run: `node tests/run-tests.js`
Expected: tests/sequence-overlay tests fail because Sequence's `resolveInsertLine(overlayEl, y)` interprets the 2nd arg as Y but the tests now pass 0 there.

### Step 4: Update Sequence implementation signature

Replace `src/ui/sequence-overlay.js:248-264` with:

```javascript
  function resolveInsertLine(overlayEl, x, y) {
    // x is accepted for signature parity with activity module; sequence's
    // single-column lifeline layout does not need horizontal disambiguation.
    if (!overlayEl) return null;
    var msgRects = overlayEl.querySelectorAll('rect[data-type="message"]');
    if (msgRects.length === 0) return null;
    var items = Array.prototype.map.call(msgRects, function(r) {
      return {
        line: parseInt(r.getAttribute('data-line'), 10),
        y: parseFloat(r.getAttribute('y')) + parseFloat(r.getAttribute('height')) / 2,
      };
    }).sort(function(a, b) { return a.y - b.y; });
    for (var i = items.length - 1; i >= 0; i--) {
      if (y > items[i].y) return { line: items[i].line, position: 'after' };
    }
    return { line: items[0].line, position: 'before' };
  }
```

(Body unchanged except for parameter list; `x` is accepted but unused.)

### Step 5: Run tests, verify PASS

Run: `node tests/run-tests.js`
Expected: All Sequence tests pass.

### Step 6: Commit

```bash
git add src/ui/sequence-overlay.js tests/sequence-overlay.test.js
git commit -m "$(cat <<'EOF'
refactor(sequence): align resolveInsertLine signature with activity

caller (src/app.js) で kind 分岐なく統一インターフェースで呼べるよう、
Sequence 側も (overlayEl, x, y) signature に変更。 X はシーケンス図の
単一 lifeline 構造では不要なため本体は無視。

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 3: `src/app.js` の caller 改修 + `drawHoverGuide` 拡張

**目的:** mousemove/click handler で X も計算して `resolveInsertLine` に渡す。 `drawHoverGuide(y)` を `drawHoverGuide(y, rectX, rectWidth)` に拡張し、 ガイド線の span を resolved rect のカラム幅に制限。

**Files:**
- Modify: `src/app.js:88-112` (`drawHoverGuide` 関数)
- Modify: `src/app.js:125-186` (mousemove/click handler ブロック)

### Step 1: Replace `drawHoverGuide` to accept optional column bounds

In `src/app.js`, replace the `drawHoverGuide` function (lines 88-112) with:

```javascript
  function drawHoverGuide(y, rectX, rectWidth) {
    clearHoverGuide();
    if (!overlayElForHover) return;
    var w = parseFloat(overlayElForHover.getAttribute('width')) || 800;
    var h = parseFloat(overlayElForHover.getAttribute('height')) || 400;
    var PADDING = 10;
    // When rectX/rectWidth are provided, constrain the guide span to the
    // resolved rect's column (with padding). Otherwise span the full overlay.
    var x1 = (typeof rectX === 'number' && typeof rectWidth === 'number')
      ? Math.max(0, rectX - PADDING)
      : 0;
    var x2 = (typeof rectX === 'number' && typeof rectWidth === 'number')
      ? Math.min(w, rectX + rectWidth + PADDING)
      : w;
    var lineEl = document.createElementNS('http://www.w3.org/2000/svg', 'line');
    lineEl.setAttribute('x1', x1);
    lineEl.setAttribute('y1', y);
    lineEl.setAttribute('x2', x2);
    lineEl.setAttribute('y2', y);
    lineEl.setAttribute('class', 'hover-guide');
    hoverEl.appendChild(lineEl);
    var text = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    text.setAttribute('x', x1 + 4);
    text.setAttribute('y', y - 3);
    text.setAttribute('class', 'hover-label');
    text.textContent = '+ ここに挿入';
    hoverEl.appendChild(text);
    hoverEl.setAttribute('width', overlayElForHover.getAttribute('width') || w);
    hoverEl.setAttribute('height', overlayElForHover.getAttribute('height') || h);
    var vb = overlayElForHover.getAttribute('viewBox');
    if (vb) hoverEl.setAttribute('viewBox', vb);
    hoverEl.style.transform = overlayElForHover.style.transform;
  }
```

### Step 2: Update mousemove handler to compute X and resolve insertion

In `src/app.js`, replace the mousemove handler block (currently around lines 126-152) with:

```javascript
    previewContainerForHover.addEventListener('mousemove', function(e) {
      // 挿入クリックを処理できるモジュール (= hoverInsert capability) でなければガイドも出さない。
      // 出すと「+ ここに挿入」が見えるのにクリックしても何も起きない誤誘導になる。
      if (!moduleHas('hoverInsert')) {
        clearHoverGuide();
        return;
      }
      // 選択中は挿入ガイドを出さない
      if (_hasSelection()) {
        clearHoverGuide();
        return;
      }
      var target = e.target;
      // overlay rect 上にマウス → guide 非表示 (既存選択を優先)
      if (target.getAttribute && target.getAttribute('data-type')) {
        clearHoverGuide();
        return;
      }
      var rect = overlayElForHover.getBoundingClientRect();
      if (e.clientX < rect.left || e.clientX > rect.right || e.clientY < rect.top || e.clientY > rect.bottom) {
        clearHoverGuide();
        return;
      }
      var z = zoom || 1;
      var x = (e.clientX - rect.left) / z;
      var y = (e.clientY - rect.top) / z;
      // Resolve via current module to obtain the column bounds (rectX/rectWidth)
      var resolver = (currentModule && typeof currentModule.resolveInsertLine === 'function')
        ? currentModule.resolveInsertLine
        : null;
      if (resolver) {
        var res = resolver(overlayElForHover, x, y);
        if (res) {
          drawHoverGuide(y, res.rectX, res.rectWidth);
          return;
        }
      }
      drawHoverGuide(y);
    });
```

### Step 3: Update click handler to pass X

In the same file, find the click handler (currently around lines 156-185) and update the `resolver(overlayElForHover, y)` call to pass X:

Before:
```javascript
      var z = zoom || 1;
      var y = (e.clientY - rect.top) / z;
      var res = resolver(overlayElForHover, y);
```

After:
```javascript
      var z = zoom || 1;
      var x = (e.clientX - rect.left) / z;
      var y = (e.clientY - rect.top) / z;
      var res = resolver(overlayElForHover, x, y);
```

Also update the resolver fallback definition. Before:
```javascript
      var resolver = (currentModule && typeof currentModule.resolveInsertLine === 'function')
        ? function(ovEl, yy) { return currentModule.resolveInsertLine(ovEl, yy); }
        : (window.MA.sequenceOverlay && window.MA.sequenceOverlay.resolveInsertLine
          ? window.MA.sequenceOverlay.resolveInsertLine
          : null);
```

After:
```javascript
      var resolver = (currentModule && typeof currentModule.resolveInsertLine === 'function')
        ? function(ovEl, xx, yy) { return currentModule.resolveInsertLine(ovEl, xx, yy); }
        : (window.MA.sequenceOverlay && window.MA.sequenceOverlay.resolveInsertLine
          ? window.MA.sequenceOverlay.resolveInsertLine
          : null);
```

### Step 4: Manual smoke test (visual verification gate)

Per CLAUDE.md visual verification gate: GUI changes need real-browser confirmation, not just unit GREEN.

Start dev server (`python server.py` in background) and open `http://127.0.0.1:8766/`.

1. Switch to `plantuml-activity`
2. Tail-add an `if` with then label `yes` else label `no`
3. After render, hover over the **else** branch column (right side) below the action rect
4. Confirm: hover guide line is **shorter** (rect-column-width) and aligned with the right column
5. Move hover to **then** column (left) — guide line jumps to the left column
6. Move to top-level area (above start or below stop) — guide line spans full overlay (or rect 1 column at least)
7. Click in the else column empty space → modal opens → confirm action insert → DSL shows action inserted in else branch

If steps 1-7 work as expected, visual verification PASS.

### Step 5: Run unit tests for regression check

Run: `node tests/run-tests.js`
Expected: All tests pass (Tasks 1+2 changes baked in).

### Step 6: Commit

```bash
git add src/app.js
git commit -m "$(cat <<'EOF'
feat(app): pass X to resolveInsertLine + column-bound hover guide

mousemove/click handler で X 座標を計算して resolveInsertLine に
渡し、 戻り値の rectX/rectWidth で hover ガイド線を resolved rect
のカラム幅に制限する。 これで「どっちの branch に入るか」が視覚的に
分かるようになる。

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 4: E2E テスト追加

**目的:** branch-aware insertion を Playwright で検証 + hover ガイド span 検証。

**Files:**
- Modify: `tests/e2e/activity.spec.js`

### Step 1: Add E2E for branch-aware insertion (UC-1) + hover guide span (UC-4)

In `tests/e2e/activity.spec.js`, add 2 tests inside `test.describe('γ: form + overlay', ...)` block, before the `console error count is 0` test:

```javascript
    test('UC-1 v1.0.2: hover guide span is constrained to resolved rect column', async ({ page }) => {
      await gotoApp(page);
      await page.locator('#diagram-type').selectOption('plantuml-activity');
      await page.waitForTimeout(2500);
      var actionRect = page.locator('#overlay-layer rect[data-type="action"]').first();
      var c = await actionRect.count();
      if (c === 0) test.skip();
      var box = await actionRect.boundingBox();
      // Hover above the action rect (so guide line appears in empty space)
      await page.mouse.move(box.x + box.width / 2, box.y - 10);
      await page.waitForTimeout(200);
      var guideX1 = await page.locator('#hover-layer line.hover-guide').first().getAttribute('x1');
      var guideX2 = await page.locator('#hover-layer line.hover-guide').first().getAttribute('x2');
      var span = parseFloat(guideX2) - parseFloat(guideX1);
      // Guide span should be limited (rect column + 2*padding ≈ 30-150px), not full overlay (>500px)
      expect(span).toBeLessThan(300);
      expect(span).toBeGreaterThan(0);
    });

    test('UC-1 v1.0.2: branch-aware click resolves to closer X column', async ({ page }) => {
      await gotoApp(page);
      await page.locator('#diagram-type').selectOption('plantuml-activity');
      await page.waitForTimeout(500);
      // Setup: top-level if with two short action branches at same Y
      await page.locator('#editor').fill('@startuml\nstart\nif (cond?) then (yes)\n  :Left;\nelse (no)\n  :Right;\nendif\nstop\n@enduml');
      await page.waitForTimeout(2500);
      var rects = page.locator('#overlay-layer rect[data-type="action"]');
      var rectCount = await rects.count();
      if (rectCount < 2) test.skip();
      // Find Left and Right rects (assume they're at the same Y, different X)
      var allBoxes = [];
      for (var i = 0; i < rectCount; i++) {
        allBoxes.push(await rects.nth(i).boundingBox());
      }
      // Identify Left and Right by X-coord (Left has smaller X)
      allBoxes.sort(function(a, b) { return a.x - b.x; });
      var leftBox = allBoxes[0];
      var rightBox = allBoxes[allBoxes.length - 1];
      if (Math.abs(leftBox.y - rightBox.y) > 30) test.skip();  // not horizontally aligned (e.g., overlay limitation)
      // Click in the gap to the right of Right rect (should resolve to Right, not Left)
      await page.mouse.click(rightBox.x + rightBox.width + 10, rightBox.y + rightBox.height / 2);
      await page.waitForTimeout(300);
      var modalDisplay = await page.locator('#act-modal').evaluate(function(el) { return el.style.display; });
      if (modalDisplay !== 'flex') test.skip();
      // Confirm modal opened with kind=action default, fill text and confirm
      await page.locator('#act-mod-text').fill('NewElseAction');
      await page.locator('#act-mod-confirm').click();
      await page.waitForTimeout(300);
      var t = await getEditorText(page);
      // The new action should be in the else branch (after :Right;)
      var lines = t.split('\n');
      var rightIdx = -1, newIdx = -1, endifIdx = -1;
      for (var j = 0; j < lines.length; j++) {
        if (lines[j].indexOf(':Right;') >= 0) rightIdx = j;
        if (lines[j].indexOf(':NewElseAction;') >= 0) newIdx = j;
        if (lines[j].trim() === 'endif') endifIdx = j;
      }
      expect(rightIdx).toBeGreaterThan(-1);
      expect(newIdx).toBeGreaterThan(-1);
      // New action should be between :Right; and endif (i.e., in else branch)
      expect(newIdx).toBeGreaterThan(rightIdx);
      expect(endifIdx).toBeGreaterThan(newIdx);
    });
```

注: E2E は buildOverlay の composite-internal action overlay rect 制限により skip フォールバックが発火する可能性がある (v1.0.1 UC-2 と同様)。 現状の E2E ランで `rectCount < 2` または `allBoxes` の Y が揃わない場合は test.skip() でフォールバック。

### Step 2: Run E2E

Start dev server: `python server.py` (background)
Run: `npx playwright test tests/e2e/activity.spec.js -g "UC-1 v1.0.2" --reporter=list`
Expected: 2 tests pass, or skip (if overlay rect not detected for the if-block — same skip pattern as v1.0.1 UC-2)

### Step 3: Run full E2E for regression

Run: `npx playwright test tests/e2e/activity.spec.js --reporter=list`
Expected: 既存 17 + 新規 2 = 19 pass / 1 skip (v1.0.1 UC-2)。 もしくは skip 数が増えるが fail はない。

### Step 4: Commit

```bash
git add tests/e2e/activity.spec.js
git commit -m "$(cat <<'EOF'
test(activity): E2E for branch-aware insertion + guide span

v1.0.2 の X-aware resolveInsertLine + hover ガイドカラム制限の検証。
UC-1 v1.0.2 (guide span) と UC-1 v1.0.2 (branch-aware click) の 2 件。
buildOverlay の composite 内 rect 制限により skip フォールバック有り。

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 5: CHANGELOG + PR

**目的:** v1.0.2 リリースノート + push + PR 作成。

**Files:**
- Modify: `CHANGELOG.md`

### Step 1: Add v1.0.2 entry

Read `CHANGELOG.md` 冒頭、`## [1.0.1]` の前に v1.0.2 セクションを挿入:

```markdown
## [1.0.2] - 2026-04-29

### Fixed — Branch-aware insertion

- **mid-insert で X 座標を考慮** — `resolveInsertLine(overlayEl, x, y)` をユークリッド距離での最近接 rect 選択に変更。 if/fork の水平分岐 composite で「else 列クリック → then 末尾に挿入される」 critical UX bug を解消
- **Hover ガイドのカラム幅制限** — 解決された rect の X 範囲 ± padding にガイド線を制限。 「どっちの branch に入るか」が視覚的に明示される
- Sequence の `resolveInsertLine` も signature を `(overlayEl, x, y)` に揃え (X は無視)、 caller (`src/app.js`) を統一インターフェース化
- Unit テスト: +4 (branch disambiguation 3 + rectX/rectWidth 1)、E2E テスト: +2 (UC-1 v1.0.2 guide span / branch-aware click)

### Notes

- 設計詳細: `docs/superpowers/specs/2026-04-29-activity-v1.0.2-design.md`
- 実装計画: `docs/superpowers/plans/2026-04-29-activity-v1.0.2.md`
- ユーザ指摘 「ここに挿入が行レベルで管理されるので分岐があるとどちらの分岐に挿入されるかわかりません」 を解消
- v1.0.3+ 繰越: buildOverlay の composite 内 action overlay rect emit (v1.0.1 UC-2 + v1.0.2 UC-1 の skip 原因)、 State 図の同種 polish

```

### Step 2: Run all tests one final time

Run: `node tests/run-tests.js` — Expected: 全 unit テスト pass
Run: `npx playwright test tests/e2e/activity.spec.js --reporter=list` — Expected: 全 E2E pass / 既知の skip のみ

### Step 3: Commit + push + PR

```bash
git add CHANGELOG.md
git commit -m "$(cat <<'EOF'
docs(changelog): v1.0.2 release notes

Branch-aware insertion fix — X 座標考慮の resolveInsertLine +
カラム制限 hover ガイド。 critical UX bug (分岐内挿入の意図外解決) を
解消。 ユーザ指摘起因の polish patch。

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
git push -u origin feat/activity-v1.0.2
gh pr create --base feat/activity-v1.0.1 --title "feat: v1.0.2 Activity branch-aware insertion" --body "$(cat <<'EOF'
## Summary

- `resolveInsertLine` を X-aware ユークリッド距離に変更、 if/fork の水平分岐 composite で意図しない branch に挿入される critical UX bug を解消
- Hover ガイド span を resolved rect のカラム幅に制限、 「どっちの branch に入るか」を視覚化
- Sequence の `resolveInsertLine` も signature 揃え (X 無視)

User feedback: 「ここに挿入が行レベルで管理されるので分岐があるとどちらの分岐に挿入されるかわかりません」 を解消。

詳細: `docs/superpowers/specs/2026-04-29-activity-v1.0.2-design.md`

## Test plan

- [x] Unit テスト 561 全 pass (前 557 + 新規 4)
- [x] E2E テスト 19 pass / 1 documented skip (v1.0.1 UC-2)
  - UC-1 v1.0.2: hover guide span 制限の検証
  - UC-1 v1.0.2: branch-aware click による else branch 挿入
- [x] Visual verification: 実機で hover ガイドが then/else カラムで切り替わる + else 列クリックで else 末尾に挿入される

## Stacked PR

Base: `feat/activity-v1.0.1` (PR #12)。 順序:
- PR #10 (v0.7.1) → master
- PR #11 (v1.0.0 Tier1 完成) → feat/activity-v0.7.1
- PR #12 (v1.0.1 Activity polish) → feat/state-v1.0.0
- **本 PR (v1.0.2 Branch-aware insertion)** → feat/activity-v1.0.1

## v1.0.3+ 繰越

- buildOverlay の composite 内 action overlay rect emit (UC-2 / UC-1 v1.0.2 skip の根本原因)
- State 図の同種 polish (mid-insert + branch-aware)
- branch 順序入れ替え (drag-to-reorder)

🤖 Generated with [Claude Code](https://claude.com/claude-code)
EOF
)"
```

---

## Self-Review Checklist (実行者向け)

各 Task 完了時に確認:

- [ ] Spec coverage:
  - § 2.A X-aware resolveInsertLine → Task 1
  - § 2.B Hover ガイドの span 制限 → Task 3
  - § 2.C 呼び出し元改修 → Task 3
  - § 3.4 Sequence signature 揃え → Task 2
  - § 4 UC-1〜UC-6 のうち UC-1/UC-3/UC-4 は Unit + E2E でカバー、UC-2/UC-5/UC-6 は Unit ロジックでカバー
  - § 5.1 Unit テスト 4 件 → Task 1 Step 1
  - § 5.2 E2E テスト 2 件 → Task 4
  - § 5.3 Visual verification → Task 3 Step 4 (manual)
- [ ] Type 整合: `resolveInsertLine(overlayEl, x, y)` の signature が Task 1 / Task 2 / Task 3 で一貫
- [ ] 戻り値フィールド: `{line, position, rectX, rectWidth}` が Task 1 で定義され Task 3 で使用、 Sequence 側は `rectX/rectWidth` を含めなくてよい (§ 3.4 で許容)
- [ ] Placeholder: なし
- [ ] commit メッセージは独立した 1 機能を表現

---

## v1.0.3+ への明示繰越 (spec § 9 と一致)

- buildOverlay の composite 内 action overlay rect emit
- State 図の同種 polish
- Sequence の multi-select connect
- branch 順序入れ替え (drag-to-reorder)
- branch 追加 modal 化 (prompt() ベースから dedicated modal へ)
