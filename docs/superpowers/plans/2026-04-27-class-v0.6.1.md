# Class v0.6.1 Polish Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Class diagram に Member 個別 SVG クリック選択と Note on class (1行+複数行 directional) を追加し、`extractMultiLineTextBBoxes` API を `core/overlay-builder.js` に追加する。

**Architecture:**
- Phase A (Note): parser 拡張 (`note (left|right|top|bottom) of ID` 1行+複数行) → formatter (`fmtNote` 自動形式判定) → ops (add/update/delete + class 削除 cascade) → buildOverlay (5点 polygon マッチング) → property panel 拡張 (tail-add note kind + class panel `+ Note` expander + notes リスト + `_renderNoteEdit`)
- Phase B (Member click): `overlay-builder.extractMultiLineTextBBoxes` (Activity v0.7.0 と共有予定の汎用 API) → `class.buildOverlay` で member rect 生成 (`data-type=member data-parent-id data-member-index`) → `selection-router` で shift+click 時に member→parentClass coerce → `renderProps` の type=member 分岐 (既存 `_renderElementEdit` に auto-expand inline edit + `scrollIntoView`)
- Phase C: E2E 6 tests + README/CHANGELOG

**Tech Stack:** JavaScript ES5 (build なし), Node native test runner, Playwright E2E, PlantUML SVG (online + local jar)

**Spec:** `docs/superpowers/specs/2026-04-27-class-v0.6.1-design.md`

**Branch:** `feat/class-v0.6.1` (派生元: `feat/tier1-class`)

---

## File Structure

### 新規ファイル
- `docs/adr/ADR-108-note-canonical-form.md` — note canonical DSL form
- `tests/e2e/class-v0.6.1.spec.js` — E2E (member click + note)

### 変更ファイル
- `src/modules/class.js` — parser + formatter + ops + buildOverlay + renderProps 拡張 (~+500 LOC)
- `src/core/overlay-builder.js` — `extractMultiLineTextBBoxes` 追加 (~+30 LOC)
- `src/core/selection-router.js` — shift+click 時の member coerce (~+15 LOC)
- `tests/class-parser.test.js` — +10 tests (note parsing)
- `tests/class-updater.test.js` — +12 tests (addNote/updateNote/deleteNote/cascade)
- `tests/class-overlay.test.js` — +5 tests (note rect + member rect)
- `tests/overlay-builder.test.js` — +3 tests (extractMultiLineTextBBoxes)
- `README.md` — v0.6.1 セクション追記
- `CHANGELOG.md` — v0.6.1 エントリ追記

---

## Phase A: Note 機能 (Tasks 0-10)

### Task 0: ADR-108 — Note canonical form

**Files:**
- Create: `docs/adr/ADR-108-note-canonical-form.md`

- [ ] **Step 1: Write ADR**

```markdown
# ADR-108: Note Canonical DSL Form

## Status
Accepted (2026-04-27)

## Context
PlantUML の `note` 構文には複数の表記揺れがある:
- 1 行: `note left of Foo : text`
- 複数行: `note left of Foo` ... `end note`
- Floating: `note "..." as N` + `Foo .. N` (v0.6.1 では out-of-scope)
- Position 大文字小文字 (`Left` / `LEFT`)
- target 省略 (`note left : text`、PlantUML として有効だが parse 困難)

## Decision
Class diagram の note canonical form:

| 形式 | canonical |
|---|---|
| 1 行 directional | `note <pos> of <ID> : <text>` (空白固定: `note SP pos SP of SP id SP : SP text`) |
| 複数行 directional | block 形式維持 (`note <pos> of <ID>\n<body>\nend note`) |
| Position 揺れ | 全部小文字 (`left` / `right` / `top` / `bottom`) |
| target 省略 | parser は受理しない (target 明示必須) |
| Floating + link | v0.6.1 out-of-scope (Tier2 / v0.7.0+) |

emit ルール: text に `\n` を含む場合は自動で複数行 (block) 形式、それ以外 1 行。

## Consequences
- 1 行 / 複数行 の自動判定で UI から position 変更や text 編集時に form を再選択する必要がない
- floating note は将来 v0.7.0 (Activity) で plain-text label と統合設計する
- target 省略を捨てたのは parser 複雑度を抑えるため (PlantUML 自体は可だが UML 図として位置不定なので実用上稀)
```

- [ ] **Step 2: Commit**

```bash
git add docs/adr/ADR-108-note-canonical-form.md
git commit -m "docs(adr): ADR-108 note canonical DSL form"
```

---

### Task 1: Parser — 1行 note

**Files:**
- Modify: `src/modules/class.js` (parser 部分、`parse()` 関数)
- Modify: `tests/class-parser.test.js`

- [ ] **Step 1: Failing tests を追加**

`tests/class-parser.test.js` の最後 (module.exports 直前) に追加:

```javascript
test('parses 1-line note left of class', () => {
  var t = '@startuml\nclass Foo\nnote left of Foo : implementation tip\n@enduml';
  var r = fns.plantumlClass.parse(t);
  assert.strictEqual(r.notes.length, 1);
  assert.strictEqual(r.notes[0].position, 'left');
  assert.strictEqual(r.notes[0].targetId, 'Foo');
  assert.strictEqual(r.notes[0].text, 'implementation tip');
  assert.strictEqual(r.notes[0].line, 3);
  assert.strictEqual(r.notes[0].endLine, 3);
});

test('parses 1-line note for all 4 positions', () => {
  var t = '@startuml\nclass A\nclass B\nclass C\nclass D\n' +
    'note left of A : la\nnote right of B : rb\nnote top of C : tc\nnote bottom of D : bd\n' +
    '@enduml';
  var r = fns.plantumlClass.parse(t);
  assert.strictEqual(r.notes.length, 4);
  assert.strictEqual(r.notes[0].position, 'left');
  assert.strictEqual(r.notes[1].position, 'right');
  assert.strictEqual(r.notes[2].position, 'top');
  assert.strictEqual(r.notes[3].position, 'bottom');
});

test('parses 1-line note for interface and abstract and enum', () => {
  var t = '@startuml\ninterface I\nabstract class A\nenum E { X }\n' +
    'note left of I : i\nnote right of A : a\nnote top of E : e\n@enduml';
  var r = fns.plantumlClass.parse(t);
  assert.strictEqual(r.notes.length, 3);
  assert.strictEqual(r.notes[0].targetId, 'I');
  assert.strictEqual(r.notes[1].targetId, 'A');
  assert.strictEqual(r.notes[2].targetId, 'E');
});

test('lower-cases position keyword', () => {
  var t = '@startuml\nclass Foo\nnote LEFT of Foo : x\n@enduml';
  var r = fns.plantumlClass.parse(t);
  assert.strictEqual(r.notes[0].position, 'left');
});

test('assigns sequential ids to notes', () => {
  var t = '@startuml\nclass Foo\nnote left of Foo : a\nnote right of Foo : b\n@enduml';
  var r = fns.plantumlClass.parse(t);
  assert.strictEqual(r.notes[0].id, '__n_0');
  assert.strictEqual(r.notes[1].id, '__n_1');
});
```

- [ ] **Step 2: Run tests to verify FAIL**

Run: `node tests/run-tests.js`
Expected: 5 new tests fail with `r.notes is undefined` or similar

- [ ] **Step 3: Implement 1-line note parsing**

`src/modules/class.js` の冒頭 regex 定数 (NAMESPACE_OPEN_RE の後) に追加:

```javascript
  var NOTE_INLINE_RE = new RegExp(
    '^note\\s+(left|right|top|bottom)\\s+of\\s+(' + ID + ')\\s*:\\s*(.*)$',
    'i'
  );
```

`parse()` 関数の戻り値 `result` 初期化を変更:

```javascript
var result = { meta: { title: '', startUmlLine: null }, elements: [], relations: [], groups: [], notes: [] };
```

`parse()` のメインループ内、`if (openClassStack.length === 0) {` ブロックの直前 (RELATION_RE match 前) に追加:

```javascript
      if (openClassStack.length === 0) {
        var noteMatch = trimmed.match(NOTE_INLINE_RE);
        if (noteMatch) {
          result.notes.push({
            kind: 'note',
            id: '__n_' + result.notes.length,
            position: noteMatch[1].toLowerCase(),
            targetId: noteMatch[2],
            text: noteMatch[3],
            line: lineNum,
            endLine: lineNum,
          });
          continue;
        }
      }
```

- [ ] **Step 4: Run tests to verify PASS**

Run: `node tests/run-tests.js`
Expected: 5 new note tests PASS, 既存の 374 unit tests も全 PASS (regression なし)

- [ ] **Step 5: Commit**

```bash
git add src/modules/class.js tests/class-parser.test.js
git commit -m "feat(class): parse 1-line note (4 directions, 4 element kinds)"
```

---

### Task 2: Parser — 複数行 note (`end note` block)

**Files:**
- Modify: `src/modules/class.js`
- Modify: `tests/class-parser.test.js`

- [ ] **Step 1: Failing tests を追加**

```javascript
test('parses multi-line note block', () => {
  var t = '@startuml\nclass Foo\nnote left of Foo\n  first line\n  second line\nend note\n@enduml';
  var r = fns.plantumlClass.parse(t);
  assert.strictEqual(r.notes.length, 1);
  assert.strictEqual(r.notes[0].position, 'left');
  assert.strictEqual(r.notes[0].targetId, 'Foo');
  assert.strictEqual(r.notes[0].text, 'first line\nsecond line');
  assert.strictEqual(r.notes[0].line, 3);
  assert.strictEqual(r.notes[0].endLine, 6);
});

test('multi-line note keeps internal blank lines', () => {
  var t = '@startuml\nclass Foo\nnote right of Foo\n  para 1\n\n  para 2\nend note\n@enduml';
  var r = fns.plantumlClass.parse(t);
  assert.strictEqual(r.notes[0].text, 'para 1\n\npara 2');
});

test('parses 1-line and multi-line notes in same diagram', () => {
  var t = '@startuml\nclass Foo\nnote left of Foo : single\n' +
    'class Bar\nnote right of Bar\n  multi\nend note\n@enduml';
  var r = fns.plantumlClass.parse(t);
  assert.strictEqual(r.notes.length, 2);
  assert.strictEqual(r.notes[0].text, 'single');
  assert.strictEqual(r.notes[1].text, 'multi');
});
```

- [ ] **Step 2: Run tests to verify FAIL**

Run: `node tests/run-tests.js`
Expected: 3 new tests fail

- [ ] **Step 3: Implement multi-line note parsing**

`src/modules/class.js` の冒頭 regex 定数に追加:

```javascript
  var NOTE_BLOCK_OPEN_RE = new RegExp(
    '^note\\s+(left|right|top|bottom)\\s+of\\s+(' + ID + ')\\s*$',
    'i'
  );
  var END_NOTE_RE = /^end\s+note\s*$/i;
```

`parse()` 内、ローカル変数を追加 (loop 直前):

```javascript
    var openNote = null;  // { startLine, position, targetId, bodyLines: [] }
```

メインループ冒頭 (空行スキップの直後) に追加:

```javascript
      // Inside multi-line note block: collect body lines
      if (openNote) {
        if (END_NOTE_RE.test(trimmed)) {
          result.notes.push({
            kind: 'note',
            id: '__n_' + result.notes.length,
            position: openNote.position,
            targetId: openNote.targetId,
            text: openNote.bodyLines.join('\n'),
            line: openNote.startLine,
            endLine: lineNum,
          });
          openNote = null;
          continue;
        }
        // Body line: preserve original (without leading 2-space indent if PlantUML style)
        openNote.bodyLines.push(lines[i].replace(/^  /, ''));
        continue;
      }
```

注意: 「空行スキップの直後」とは `if (!trimmed || DU.isPlantumlComment(trimmed)) continue;` の直後だが、note block 内では空行を保持する必要がある。空行スキップ判定を openNote 状態で分岐:

```javascript
      var lineNum = i + 1;
      var rawLine = lines[i];
      var trimmed = rawLine.trim();

      // Inside multi-line note block: handle BEFORE empty/comment skip
      if (openNote) {
        if (END_NOTE_RE.test(trimmed)) {
          result.notes.push({
            kind: 'note',
            id: '__n_' + result.notes.length,
            position: openNote.position,
            targetId: openNote.targetId,
            text: openNote.bodyLines.join('\n'),
            line: openNote.startLine,
            endLine: lineNum,
          });
          openNote = null;
          continue;
        }
        openNote.bodyLines.push(rawLine.replace(/^  /, ''));
        continue;
      }

      if (!trimmed || DU.isPlantumlComment(trimmed)) continue;
```

`parse()` 内、INLINE note check の後 (RELATION_RE match の前) に追加:

```javascript
        var blockMatch = trimmed.match(NOTE_BLOCK_OPEN_RE);
        if (blockMatch) {
          openNote = {
            startLine: lineNum,
            position: blockMatch[1].toLowerCase(),
            targetId: blockMatch[2],
            bodyLines: [],
          };
          continue;
        }
```

- [ ] **Step 4: Run tests to verify PASS**

Run: `node tests/run-tests.js`
Expected: 3 new tests + 既存 5 inline tests + 既存 374 全 PASS

- [ ] **Step 5: Commit**

```bash
git add src/modules/class.js tests/class-parser.test.js
git commit -m "feat(class): parse multi-line note block (end note)"
```

---

### Task 3: Formatter — fmtNote (1行/複数行 自動判定)

**Files:**
- Modify: `src/modules/class.js`
- Modify: `tests/class-updater.test.js`

- [ ] **Step 1: Failing tests を追加**

`tests/class-updater.test.js` の最後に:

```javascript
test('fmtNote 1-line for short single-line text', () => {
  var line = fns.plantumlClass.fmtNote('left', 'Foo', 'short tip');
  assert.strictEqual(line, 'note left of Foo : short tip');
});

test('fmtNote multi-line block for text with newline', () => {
  var lines = fns.plantumlClass.fmtNote('right', 'Bar', 'first\nsecond');
  assert.deepStrictEqual(lines, ['note right of Bar', 'first', 'second', 'end note']);
});

test('fmtNote 1-line preserves spaces in text', () => {
  var line = fns.plantumlClass.fmtNote('top', 'Foo', 'a b  c');
  assert.strictEqual(line, 'note top of Foo : a b  c');
});

test('fmtNote multi-line preserves blank lines', () => {
  var lines = fns.plantumlClass.fmtNote('left', 'Foo', 'a\n\nb');
  assert.deepStrictEqual(lines, ['note left of Foo', 'a', '', 'b', 'end note']);
});
```

- [ ] **Step 2: Run tests to verify FAIL**

Run: `node tests/run-tests.js`

- [ ] **Step 3: Implement fmtNote**

`src/modules/class.js` の `fmtNamespace` の直後に追加:

```javascript
  function fmtNote(position, targetId, text) {
    var pos = (position || 'left').toLowerCase();
    if (typeof text !== 'string') text = '';
    if (text.indexOf('\n') < 0) {
      return 'note ' + pos + ' of ' + targetId + ' : ' + text;
    }
    var bodyLines = text.split('\n');
    var out = ['note ' + pos + ' of ' + targetId];
    bodyLines.forEach(function(l) { out.push(l); });
    out.push('end note');
    return out;
  }
```

module の return に `fmtNote: fmtNote,` を追加 (export 一覧の適切な位置に):

```javascript
  return {
    parse: parse,
    fmtClass: fmtClass,
    fmtInterface: fmtInterface,
    fmtAbstract: fmtAbstract,
    fmtEnum: fmtEnum,
    fmtRelation: fmtRelation,
    fmtAttribute: fmtAttribute,
    fmtMethod: fmtMethod,
    fmtEnumValue: fmtEnumValue,
    fmtPackage: fmtPackage,
    fmtNamespace: fmtNamespace,
    fmtNote: fmtNote,                       // ← new
    addClass: addClass,
    // ... 以下既存
```

- [ ] **Step 4: Run tests to verify PASS**

Run: `node tests/run-tests.js`

- [ ] **Step 5: Commit**

```bash
git add src/modules/class.js tests/class-updater.test.js
git commit -m "feat(class): fmtNote (1-line vs multi-line auto)"
```

---

### Task 4: Op — addNote

**Files:**
- Modify: `src/modules/class.js`
- Modify: `tests/class-updater.test.js`

- [ ] **Step 1: Failing tests を追加**

```javascript
test('addNote inserts 1-line note before @enduml', () => {
  var t = '@startuml\nclass Foo\n@enduml';
  var out = fns.plantumlClass.addNote(t, 'Foo', 'left', 'tip');
  assert.match(out, /note left of Foo : tip/);
  assert.match(out, /@enduml/);
});

test('addNote inserts multi-line block when text has newline', () => {
  var t = '@startuml\nclass Foo\n@enduml';
  var out = fns.plantumlClass.addNote(t, 'Foo', 'right', 'a\nb');
  var ls = out.split('\n');
  assert.ok(ls.indexOf('note right of Foo') >= 0);
  assert.ok(ls.indexOf('end note') >= 0);
});

test('addNote default position is left', () => {
  var t = '@startuml\nclass Foo\n@enduml';
  var out = fns.plantumlClass.addNote(t, 'Foo', null, 'x');
  assert.match(out, /note left of Foo : x/);
});
```

- [ ] **Step 2: Run tests to verify FAIL**

- [ ] **Step 3: Implement addNote**

`src/modules/class.js` の add ops (addNamespace の後) に追加:

```javascript
  function addNote(text, targetId, position, noteText) {
    var pos = position || 'left';
    var formatted = fmtNote(pos, targetId, noteText || '');
    if (Array.isArray(formatted)) {
      var out = text;
      formatted.forEach(function(l) { out = insertBeforeEnd(out, l); });
      return out;
    }
    return insertBeforeEnd(text, formatted);
  }
```

module export に追加:

```javascript
    addPackage: addPackage,
    addNamespace: addNamespace,
    addNote: addNote,                       // ← new
```

- [ ] **Step 4: Run tests to verify PASS**

- [ ] **Step 5: Commit**

```bash
git add src/modules/class.js tests/class-updater.test.js
git commit -m "feat(class): addNote (1-line/multi-line auto-detect)"
```

---

### Task 5: Op — updateNote (position + text)

**Files:**
- Modify: `src/modules/class.js`
- Modify: `tests/class-updater.test.js`

- [ ] **Step 1: Failing tests を追加**

```javascript
test('updateNote changes position of 1-line note', () => {
  var t = '@startuml\nclass Foo\nnote left of Foo : tip\n@enduml';
  var out = fns.plantumlClass.updateNote(t, 3, 3, { position: 'right' });
  assert.match(out, /note right of Foo : tip/);
});

test('updateNote changes text of 1-line note', () => {
  var t = '@startuml\nclass Foo\nnote left of Foo : old\n@enduml';
  var out = fns.plantumlClass.updateNote(t, 3, 3, { text: 'new' });
  assert.match(out, /note left of Foo : new/);
});

test('updateNote converts 1-line to multi-line when text has newline', () => {
  var t = '@startuml\nclass Foo\nnote left of Foo : single\n@enduml';
  var out = fns.plantumlClass.updateNote(t, 3, 3, { text: 'a\nb' });
  var ls = out.split('\n');
  assert.ok(ls.indexOf('note left of Foo') >= 0);
  assert.ok(ls.indexOf('end note') >= 0);
});

test('updateNote replaces multi-line block', () => {
  var t = '@startuml\nclass Foo\nnote left of Foo\n  old1\n  old2\nend note\n@enduml';
  var out = fns.plantumlClass.updateNote(t, 3, 6, { text: 'new1\nnew2' });
  assert.match(out, /new1/);
  assert.match(out, /new2/);
  assert.doesNotMatch(out, /old1/);
});
```

- [ ] **Step 2: Run tests to verify FAIL**

- [ ] **Step 3: Implement updateNote**

`src/modules/class.js` の update ops (updateRelation の後) に追加:

```javascript
  function updateNote(text, startLine, endLine, fields) {
    var lines = text.split('\n');
    var startIdx = startLine - 1;
    var endIdx = endLine - 1;
    if (startIdx < 0 || startIdx >= lines.length) return text;

    // Parse current note state from start line
    var startTrimmed = lines[startIdx].trim();
    var inlineMatch = startTrimmed.match(NOTE_INLINE_RE);
    var blockMatch = startTrimmed.match(NOTE_BLOCK_OPEN_RE);
    var current = null;
    if (inlineMatch) {
      current = { position: inlineMatch[1].toLowerCase(), targetId: inlineMatch[2], text: inlineMatch[3] };
    } else if (blockMatch) {
      var bodyLines = [];
      for (var k = startIdx + 1; k <= endIdx - 1; k++) {
        bodyLines.push(lines[k].replace(/^  /, ''));
      }
      current = { position: blockMatch[1].toLowerCase(), targetId: blockMatch[2], text: bodyLines.join('\n') };
    }
    if (!current) return text;

    var newPos = fields.position != null ? fields.position : current.position;
    var newText = fields.text != null ? fields.text : current.text;

    var formatted = fmtNote(newPos, current.targetId, newText);
    var newLines;
    if (Array.isArray(formatted)) {
      newLines = formatted;
    } else {
      newLines = [formatted];
    }

    // Replace [startIdx..endIdx] with newLines
    var before = lines.slice(0, startIdx);
    var after = lines.slice(endIdx + 1);
    return before.concat(newLines).concat(after).join('\n');
  }
```

module export に追加:

```javascript
    updateRelation: updateRelation,
    updateNote: updateNote,                 // ← new
```

- [ ] **Step 4: Run tests to verify PASS**

- [ ] **Step 5: Commit**

```bash
git add src/modules/class.js tests/class-updater.test.js
git commit -m "feat(class): updateNote (position + text, 1-line ⇄ multi-line)"
```

---

### Task 6: Op — deleteNote (single)

**Files:**
- Modify: `src/modules/class.js`
- Modify: `tests/class-updater.test.js`

- [ ] **Step 1: Failing tests を追加**

```javascript
test('deleteNote removes 1-line note', () => {
  var t = '@startuml\nclass Foo\nnote left of Foo : x\n@enduml';
  var out = fns.plantumlClass.deleteNote(t, 3, 3);
  assert.doesNotMatch(out, /note left of Foo/);
  assert.match(out, /class Foo/);
});

test('deleteNote removes multi-line block', () => {
  var t = '@startuml\nclass Foo\nnote left of Foo\n  body\nend note\n@enduml';
  var out = fns.plantumlClass.deleteNote(t, 3, 5);
  assert.doesNotMatch(out, /note left/);
  assert.doesNotMatch(out, /end note/);
  assert.doesNotMatch(out, /body/);
});

test('deleteNote preserves surrounding lines', () => {
  var t = '@startuml\nclass A\nnote left of A : tip\nclass B\n@enduml';
  var out = fns.plantumlClass.deleteNote(t, 3, 3);
  assert.match(out, /class A/);
  assert.match(out, /class B/);
});
```

- [ ] **Step 2: Run tests to verify FAIL**

- [ ] **Step 3: Implement deleteNote**

`src/modules/class.js` の updateNote の直後に追加:

```javascript
  function deleteNote(text, startLine, endLine) {
    var lines = text.split('\n');
    var startIdx = startLine - 1;
    var endIdx = endLine - 1;
    if (startIdx < 0 || startIdx >= lines.length) return text;
    var before = lines.slice(0, startIdx);
    var after = lines.slice(endIdx + 1);
    return before.concat(after).join('\n');
  }
```

module export に追加:

```javascript
    updateNote: updateNote,
    deleteNote: deleteNote,                 // ← new
```

- [ ] **Step 4: Run tests to verify PASS**

- [ ] **Step 5: Commit**

```bash
git add src/modules/class.js tests/class-updater.test.js
git commit -m "feat(class): deleteNote (1-line + multi-line block)"
```

---

### Task 7: Op — Cascade delete on element delete

**Files:**
- Modify: `src/modules/class.js` (deleteLine 関数の class/interface/abstract/enum 削除パスに cascade 追加)
- Modify: `tests/class-updater.test.js`

- [ ] **Step 1: Failing tests を追加**

```javascript
test('deleting a class also removes its notes', () => {
  var t = '@startuml\nclass Foo\nnote left of Foo : a\nnote right of Foo : b\nclass Bar\n@enduml';
  // Foo は line 2 に存在 (block なしなので endLine = 2)
  var out = fns.plantumlClass.deleteClassWithNotes(t, 'Foo');
  assert.doesNotMatch(out, /class Foo/);
  assert.doesNotMatch(out, /note left of Foo/);
  assert.doesNotMatch(out, /note right of Foo/);
  assert.match(out, /class Bar/);
});

test('deleting a class with multi-line notes removes the entire note blocks', () => {
  var t = '@startuml\nclass Foo\nnote left of Foo\n  body\nend note\nclass Bar\n@enduml';
  var out = fns.plantumlClass.deleteClassWithNotes(t, 'Foo');
  assert.doesNotMatch(out, /class Foo/);
  assert.doesNotMatch(out, /note left of Foo/);
  assert.doesNotMatch(out, /end note/);
  assert.doesNotMatch(out, /body/);
});

test('deleteClassWithNotes preserves notes targeting other classes', () => {
  var t = '@startuml\nclass Foo\nclass Bar\nnote left of Bar : keep\n@enduml';
  var out = fns.plantumlClass.deleteClassWithNotes(t, 'Foo');
  assert.doesNotMatch(out, /class Foo/);
  assert.match(out, /note left of Bar : keep/);
});
```

- [ ] **Step 2: Run tests to verify FAIL**

- [ ] **Step 3: Implement deleteClassWithNotes**

`src/modules/class.js` に追加 (deleteLine の後):

```javascript
  function deleteClassWithNotes(text, classId) {
    var parsed = parse(text);
    // Find target element
    var elt = null;
    for (var i = 0; i < parsed.elements.length; i++) {
      if (parsed.elements[i].id === classId) { elt = parsed.elements[i]; break; }
    }
    if (!elt) return text;

    // Collect line ranges to delete: element + its notes (descending order to avoid index shift)
    var ranges = [];
    var elStart = elt.line;
    var elEnd = elt.endLine && elt.endLine > elt.line ? elt.endLine : elt.line;
    ranges.push({ start: elStart, end: elEnd });
    parsed.notes.forEach(function(n) {
      if (n.targetId === classId) {
        ranges.push({ start: n.line, end: n.endLine });
      }
    });
    ranges.sort(function(a, b) { return b.start - a.start; });

    var lines = text.split('\n');
    ranges.forEach(function(r) {
      var startIdx = r.start - 1;
      var endIdx = r.end - 1;
      lines.splice(startIdx, endIdx - startIdx + 1);
    });
    return lines.join('\n');
  }
```

module export に追加:

```javascript
    deleteNote: deleteNote,
    deleteClassWithNotes: deleteClassWithNotes,    // ← new
```

renderProps の class 削除 button (`cl-delete`) を `deleteClassWithNotes` に切替:

`_renderElementEdit` の delete handler を探して書き換え。具体的には現在 (`src/modules/class.js` Line 782-788 付近):

```javascript
    P.bindEvent('cl-delete', 'click', function() {
      if (!confirm('この行を削除しますか？')) return;
      window.MA.history.pushHistory();
      ctx.setMmdText(deleteLine(ctx.getMmdText(), element.line));
      window.MA.selection.clearSelection();
      ctx.onUpdate();
    });
```

を以下に変更:

```javascript
    P.bindEvent('cl-delete', 'click', function() {
      if (!confirm('このクラスと紐付く note も削除します。続行しますか？')) return;
      window.MA.history.pushHistory();
      ctx.setMmdText(deleteClassWithNotes(ctx.getMmdText(), element.id));
      window.MA.selection.clearSelection();
      ctx.onUpdate();
    });
```

`_renderEnumEdit` 内の同様 `cl-delete` handler (`src/modules/class.js` Line 884 付近) も同じ修正。relation の `cl-delete` (Line 947 付近) は対象外 (note は relation に紐付かない)。

- [ ] **Step 4: Run tests to verify PASS**

Run: `node tests/run-tests.js`
Expected: 3 new tests pass。既存 374 + 新規 (5+3+4+3+4+3+3 = 25) = 399 全 PASS

- [ ] **Step 5: Commit**

```bash
git add src/modules/class.js tests/class-updater.test.js
git commit -m "feat(class): cascade delete notes when class deleted"
```

---

### Task 8: buildOverlay — note rect (5点 polygon マッチング)

**Files:**
- Modify: `src/modules/class.js` (buildOverlay 関数)
- Modify: `tests/class-overlay.test.js`

- [ ] **Step 1: Failing tests を追加**

`tests/class-overlay.test.js` の最後に:

```javascript
test('buildOverlay creates rect for note when polygon matches', () => {
  var dom = makeDom();
  var svg = dom.window.document.createElementNS('http://www.w3.org/2000/svg', 'svg');
  // class entity
  var ent = dom.window.document.createElementNS('http://www.w3.org/2000/svg', 'g');
  ent.setAttribute('class', 'entity');
  ent.setAttribute('data-qualified-name', 'Foo');
  var rect = dom.window.document.createElementNS('http://www.w3.org/2000/svg', 'rect');
  rect.setAttribute('x', '10'); rect.setAttribute('y', '10');
  rect.setAttribute('width', '100'); rect.setAttribute('height', '40');
  ent.appendChild(rect);
  svg.appendChild(ent);
  // note polygon (5 points)
  var poly = dom.window.document.createElementNS('http://www.w3.org/2000/svg', 'polygon');
  poly.setAttribute('points', '200,10 290,10 300,20 300,50 200,50');
  svg.appendChild(poly);

  var overlay = dom.window.document.createElementNS('http://www.w3.org/2000/svg', 'svg');
  var parsed = {
    elements: [{ kind: 'class', id: 'Foo', label: 'Foo', members: [], line: 2, endLine: 2 }],
    relations: [],
    groups: [],
    notes: [{ id: '__n_0', targetId: 'Foo', position: 'right', line: 3, endLine: 3, text: 'tip' }],
  };
  fns.plantumlClass.buildOverlay(svg, overlay, parsed);
  var noteRects = overlay.querySelectorAll('rect[data-type="note"]');
  assert.strictEqual(noteRects.length, 1);
  assert.strictEqual(noteRects[0].getAttribute('data-id'), '__n_0');
});

test('buildOverlay omits note rect when polygon count mismatches notes count', () => {
  var dom = makeDom();
  var svg = dom.window.document.createElementNS('http://www.w3.org/2000/svg', 'svg');
  var ent = dom.window.document.createElementNS('http://www.w3.org/2000/svg', 'g');
  ent.setAttribute('class', 'entity');
  ent.setAttribute('data-qualified-name', 'Foo');
  svg.appendChild(ent);
  // No note polygons in SVG
  var overlay = dom.window.document.createElementNS('http://www.w3.org/2000/svg', 'svg');
  var parsed = {
    elements: [{ kind: 'class', id: 'Foo', label: 'Foo', members: [], line: 2, endLine: 2 }],
    relations: [], groups: [],
    notes: [{ id: '__n_0', targetId: 'Foo', position: 'right', line: 3, endLine: 3, text: 'tip' }],
  };
  fns.plantumlClass.buildOverlay(svg, overlay, parsed);
  var noteRects = overlay.querySelectorAll('rect[data-type="note"]');
  assert.strictEqual(noteRects.length, 0);
});
```

注: `makeDom` は既存 test file 上部にあるはず。なければ既存 tests を参考に作る。

- [ ] **Step 2: Run tests to verify FAIL**

- [ ] **Step 3: Implement note rect generation in buildOverlay**

`src/modules/class.js` の `buildOverlay` 関数の末尾 (return 直前) に追加:

```javascript
    // Notes: match 5-point polygons in document order against parsed notes
    var notes = parsedData.notes || [];
    if (notes.length > 0) {
      var allPolys = svgEl.querySelectorAll('polygon');
      var notePolys = [];
      Array.prototype.forEach.call(allPolys, function(p) {
        var pts = (p.getAttribute('points') || '').trim().split(/\s+/);
        if (pts.length === 5) notePolys.push(p);
      });
      if (notePolys.length === notes.length) {
        notes.forEach(function(n, idx) {
          var p = notePolys[idx];
          var bb = OB.extractEdgeBBox(p, 0);
          if (!bb) return;
          OB.addRect(overlayEl, bb.x, bb.y, bb.width, bb.height, {
            'data-type': 'note',
            'data-id': n.id,
            'data-line': n.line,
            'data-target-id': n.targetId,
          });
        });
      } else if (typeof console !== 'undefined' && console.warn) {
        console.warn('[class.buildOverlay] note polygon count mismatch: model=' + notes.length + ' svg=' + notePolys.length);
      }
    }
```

(`OB` は buildOverlay 関数冒頭で `var OB = window.MA.overlayBuilder;` のように既に定義されている前提。なければ追加)

- [ ] **Step 4: Run tests to verify PASS**

- [ ] **Step 5: Commit**

```bash
git add src/modules/class.js tests/class-overlay.test.js
git commit -m "feat(class): buildOverlay note rect (5-point polygon match)"
```

---

### Task 9: renderProps — tail-add 'note' kind option

**Files:**
- Modify: `src/modules/class.js` (`_renderNoSelection` 関数)

- [ ] **Step 1: Add 'note' to tail-add kind selector**

`_renderNoSelection` 内、kind selector options に追加:

```javascript
        P.selectFieldHtml('種類', 'cl-tail-kind', [
          { value: 'class',     label: 'Class', selected: true },
          { value: 'interface', label: 'Interface' },
          { value: 'abstract',  label: 'Abstract Class' },
          { value: 'enum',      label: 'Enum' },
          { value: 'package',   label: 'Package境界' },
          { value: 'namespace', label: 'Namespace' },
          { value: 'relation',  label: 'Relation (関係)' },
          { value: 'note',      label: 'Note (注釈)' },     // ← new
        ]) +
```

`renderTailDetail` 内の kind 分岐に note 用 form を追加 (relation の後、関数閉じ括弧の前):

```javascript
      } else if (kind === 'note') {
        var noteTargets = elements.map(function(e) { return { value: e.id, label: e.label || e.id }; });
        if (noteTargets.length === 0) noteTargets = [{ value: '', label: '（要素なし）' }];
        html2 =
          P.selectFieldHtml('Target', 'cl-tail-ntarget', noteTargets) +
          P.selectFieldHtml('Position', 'cl-tail-npos', [
            { value: 'left', label: 'Left', selected: true },
            { value: 'right', label: 'Right' },
            { value: 'top', label: 'Top' },
            { value: 'bottom', label: 'Bottom' },
          ]) +
          '<div style="margin-bottom:6px;">' +
            '<label style="display:block;font-size:10px;color:var(--text-secondary);margin-bottom:2px;">Text (改行可)</label>' +
            '<textarea id="cl-tail-ntext" style="width:100%;min-height:60px;font-family:inherit;font-size:12px;"></textarea>' +
          '</div>' +
          P.primaryButtonHtml('cl-tail-add', '+ Note 追加');
      }
```

`P.bindEvent('cl-tail-add', 'click', function() {` ハンドラ内、k 分岐に追加:

```javascript
        } else if (k === 'note') {
          var ntg = document.getElementById('cl-tail-ntarget').value;
          if (!ntg) { alert('Target 必須'); return; }
          var npos = document.getElementById('cl-tail-npos').value;
          var ntext = document.getElementById('cl-tail-ntext').value || '';
          out = addNote(t, ntg, npos, ntext);
        }
```

- [ ] **Step 2: Manual smoke test**

Run: `python server.py` (background) → http://127.0.0.1:8766
- diagram type を Class に切替
- 末尾に追加 → 種類 = Note → target=User → text="tip" → [+] → editor に `note left of User : tip` が追加されることを確認

- [ ] **Step 3: Commit**

```bash
git add src/modules/class.js
git commit -m "feat(class): tail-add note kind (target + position + textarea)"
```

---

### Task 10: renderProps — class panel `+ Note` expander + notes list + `_renderNoteEdit`

**Files:**
- Modify: `src/modules/class.js`

- [ ] **Step 1: Add notes section + Note expander to `_renderElementEdit`**

`_renderElementEdit` 関数内、`[+ Method 追加]` button の後 (`[↑ 上へ] [↓ 下へ] [✕ 削除]` の前) に追加:

```javascript
    // Notes section
    var classNotes = (parsedData.notes || []).filter(function(n) { return n.targetId === element.id; });
    html += '<div style="border-top:1px solid var(--border);padding-top:8px;margin-top:8px;">' +
            '<div style="font-size:10px;color:var(--accent);font-weight:bold;margin-bottom:4px;">Notes</div>';
    if (classNotes.length === 0) {
      html += '<div style="font-size:11px;color:var(--text-secondary);font-style:italic;">（このクラスへの note なし）</div>';
    } else {
      classNotes.forEach(function(n, idx) {
        var preview = (n.text || '').replace(/\n/g, ' ⏎ ').slice(0, 40);
        if ((n.text || '').length > 40) preview += '...';
        html += '<div style="display:flex;align-items:center;gap:4px;font-size:11px;margin-bottom:2px;">' +
                  '<span style="flex:1;">' + n.position + ' "' + preview.replace(/[<>&]/g, '') + '" (L' + n.line + ')</span>' +
                  '<button id="cl-note-edit-' + idx + '" data-line="' + n.line + '" data-end="' + n.endLine + '" data-id="' + n.id + '">edit</button>' +
                  '<button id="cl-note-del-' + idx + '" data-line="' + n.line + '" data-end="' + n.endLine + '">✕</button>' +
                '</div>';
      });
    }
    html += '<div id="cl-add-note-form" style="margin-top:6px;"></div>' +
            '<button id="cl-add-note-btn" style="margin-top:4px;">+ Note 追加</button>' +
          '</div>';
```

(`html` 変数は `_renderElementEdit` 全体で組み立てられ最後に `propsEl.innerHTML = html` される。上記コードを適切な位置に挿入)

`propsEl.innerHTML = html;` の後に bind handlers 追加:

```javascript
    classNotes.forEach(function(n, idx) {
      P.bindEvent('cl-note-edit-' + idx, 'click', function(e) {
        var btn = e.currentTarget;
        var ln = parseInt(btn.getAttribute('data-line'), 10);
        // Switch to note selection
        ctx.setSelectedHighlight([{ type: 'note', id: btn.getAttribute('data-id'), line: ln }]);
        ctx.onUpdate();
      });
      P.bindEvent('cl-note-del-' + idx, 'click', function(e) {
        var btn = e.currentTarget;
        var sl = parseInt(btn.getAttribute('data-line'), 10);
        var el = parseInt(btn.getAttribute('data-end'), 10);
        window.MA.history.pushHistory();
        ctx.setMmdText(deleteNote(ctx.getMmdText(), sl, el));
        ctx.onUpdate();
      });
    });

    P.bindEvent('cl-add-note-btn', 'click', function() {
      var f = document.getElementById('cl-add-note-form');
      f.innerHTML =
        P.selectFieldHtml('Position', 'cl-new-npos', [
          { value: 'left', label: 'Left', selected: true },
          { value: 'right', label: 'Right' },
          { value: 'top', label: 'Top' },
          { value: 'bottom', label: 'Bottom' },
        ]) +
        '<div style="margin-bottom:6px;">' +
          '<label style="display:block;font-size:10px;color:var(--text-secondary);">Text</label>' +
          '<textarea id="cl-new-ntext" style="width:100%;min-height:50px;"></textarea>' +
        '</div>' +
        P.primaryButtonHtml('cl-new-nadd', '+ 追加');
      P.bindEvent('cl-new-nadd', 'click', function() {
        var pos = document.getElementById('cl-new-npos').value;
        var txt = document.getElementById('cl-new-ntext').value || '';
        window.MA.history.pushHistory();
        ctx.setMmdText(addNote(ctx.getMmdText(), element.id, pos, txt));
        ctx.onUpdate();
      });
    });
```

- [ ] **Step 2: Add `_renderNoteEdit` function**

`_renderRelationEdit` の直後に追加:

```javascript
  function _renderNoteEdit(note, parsedData, propsEl, ctx) {
    var P = window.MA.properties;
    var html =
      '<div style="margin-bottom:8px;font-size:11px;color:var(--text-secondary);">Note (target: ' + note.targetId + ', L' + note.line + ')</div>' +
      '<div style="margin-bottom:6px;font-size:11px;"><b>Target:</b> ' + note.targetId + ' <span style="color:var(--text-secondary);">(read-only)</span></div>' +
      P.selectFieldHtml('Position', 'cl-note-pos', [
        { value: 'left', label: 'Left', selected: note.position === 'left' },
        { value: 'right', label: 'Right', selected: note.position === 'right' },
        { value: 'top', label: 'Top', selected: note.position === 'top' },
        { value: 'bottom', label: 'Bottom', selected: note.position === 'bottom' },
      ]) +
      '<div style="margin-bottom:6px;">' +
        '<label style="display:block;font-size:10px;color:var(--text-secondary);">Text</label>' +
        '<textarea id="cl-note-text" style="width:100%;min-height:80px;">' + (note.text || '').replace(/[<>&]/g, '') + '</textarea>' +
      '</div>' +
      P.primaryButtonHtml('cl-note-update', '更新') +
      P.primaryButtonHtml('cl-note-delete', '✕ 削除');
    propsEl.innerHTML = html;

    P.bindEvent('cl-note-update', 'click', function() {
      var pos = document.getElementById('cl-note-pos').value;
      var txt = document.getElementById('cl-note-text').value;
      window.MA.history.pushHistory();
      ctx.setMmdText(updateNote(ctx.getMmdText(), note.line, note.endLine, { position: pos, text: txt }));
      ctx.onUpdate();
    });
    P.bindEvent('cl-note-delete', 'click', function() {
      window.MA.history.pushHistory();
      ctx.setMmdText(deleteNote(ctx.getMmdText(), note.line, note.endLine));
      ctx.setSelectedHighlight([]);
      ctx.onUpdate();
    });
  }
```

- [ ] **Step 3: Wire `_renderNoteEdit` into renderByDispatch**

`renderProps` の dispatch オプションに onNote を追加。`renderByDispatch` がカスタム type に対応していなければ、renderProps 関数で先に note を判定:

```javascript
  function renderProps(selData, parsedData, propsEl, ctx) {
    if (!propsEl) return;
    // Custom dispatch for 'note' selection
    if (selData && selData.length === 1 && selData[0].type === 'note') {
      var note = (parsedData.notes || []).find(function(n) { return n.id === selData[0].id; });
      if (note) {
        _renderNoteEdit(note, parsedData, propsEl, ctx);
        return;
      }
    }
    window.MA.propsRenderer.renderByDispatch(selData, parsedData, propsEl, {
      onNoSelection: function(p, e) { _renderNoSelection(p, e, ctx); },
      onElement: function(elt, p, e) { _renderElementEdit(elt, p, e, ctx); },
      onRelation: function(rel, p, e) { _renderRelationEdit(rel, p, e, ctx); },
      onGroup: function(grp, p, e) { _renderGroupReadOnly(grp, p, e, ctx); },
      onMultiSelectConnect: function(s, p, e) { _renderMultiSelectConnect(s, p, e, ctx); },
      onMultiSelect: function(s, p, e) { _renderMultiSelect(s, p, e); },
    });
  }
```

注: ES5 環境で Array.prototype.find は polyfill 不要 (jsdom 含め一般的)。fail safe なら `var note = null; for (var i=0; i<parsedData.notes.length; i++) { if (parsedData.notes[i].id === selData[0].id) { note = parsedData.notes[i]; break; } }` で。

- [ ] **Step 4: Manual smoke test**

`python server.py` 起動 → Class 図 → class 選択 → 「+ Note 追加」expander で position=Right + text="hello" → [+] → editor に `note right of <id> : hello` が追加。次に notes list で edit click → `_renderNoteEdit` panel で position=Top に変更 → 更新 → editor が `note top of` に。✕ で削除確認。

- [ ] **Step 5: Commit**

```bash
git add src/modules/class.js
git commit -m "feat(class): class panel + Note expander + notes list + _renderNoteEdit"
```

---

## Phase B: Member 個別 click (Tasks 11-14)

### Task 11: overlay-builder — `extractMultiLineTextBBoxes` API + 単体 tests

**Files:**
- Modify: `src/core/overlay-builder.js`
- Modify: `tests/overlay-builder.test.js`

- [ ] **Step 1: Failing tests を追加**

`tests/overlay-builder.test.js` の最後に:

```javascript
test('extractMultiLineTextBBoxes returns one entry per <text> in text-per-line mode', () => {
  var dom = makeDom();
  var doc = dom.window.document;
  var g = doc.createElementNS('http://www.w3.org/2000/svg', 'g');
  for (var i = 0; i < 3; i++) {
    var t = doc.createElementNS('http://www.w3.org/2000/svg', 'text');
    t.setAttribute('x', String(10 + i)); t.setAttribute('y', String(20 + i * 10));
    t.textContent = 'line' + i;
    g.appendChild(t);
  }
  var lines = fns.overlayBuilder.extractMultiLineTextBBoxes(g);
  assert.strictEqual(lines.length, 3);
  assert.strictEqual(lines[0].text, 'line0');
  assert.strictEqual(lines[2].lineIndex, 2);
});

test('extractMultiLineTextBBoxes returns empty array for empty group', () => {
  var dom = makeDom();
  var g = dom.window.document.createElementNS('http://www.w3.org/2000/svg', 'g');
  var lines = fns.overlayBuilder.extractMultiLineTextBBoxes(g);
  assert.strictEqual(lines.length, 0);
});

test('extractMultiLineTextBBoxes uses tspan-per-line mode', () => {
  var dom = makeDom();
  var doc = dom.window.document;
  var g = doc.createElementNS('http://www.w3.org/2000/svg', 'g');
  var t = doc.createElementNS('http://www.w3.org/2000/svg', 'text');
  for (var i = 0; i < 2; i++) {
    var ts = doc.createElementNS('http://www.w3.org/2000/svg', 'tspan');
    ts.textContent = 'span' + i;
    t.appendChild(ts);
  }
  g.appendChild(t);
  var lines = fns.overlayBuilder.extractMultiLineTextBBoxes(g, { mode: 'tspan-per-line' });
  assert.strictEqual(lines.length, 2);
  assert.strictEqual(lines[0].text, 'span0');
  assert.strictEqual(lines[1].text, 'span1');
});
```

- [ ] **Step 2: Run tests to verify FAIL**

- [ ] **Step 3: Implement `extractMultiLineTextBBoxes`**

`src/core/overlay-builder.js` の `extractEdgeBBox` の後に追加:

```javascript
  function extractMultiLineTextBBoxes(g, opts) {
    if (!g) return [];
    var mode = (opts && opts.mode) || 'text-per-line';
    var nodes = mode === 'tspan-per-line'
      ? g.querySelectorAll('text tspan')
      : g.querySelectorAll('text');
    var lines = [];
    for (var i = 0; i < nodes.length; i++) {
      var n = nodes[i];
      var bb = null;
      if (typeof n.getBBox === 'function') {
        try { bb = n.getBBox(); } catch (e) { /* jsdom fallback */ }
      }
      if (!bb || (!bb.width && !bb.height)) {
        bb = {
          x: parseFloat(n.getAttribute('x')) || 0,
          y: parseFloat(n.getAttribute('y')) || 0,
          width: parseFloat(n.getAttribute('textLength')) || (n.textContent || '').length * 7,
          height: 14,
        };
      }
      lines.push({ text: (n.textContent || '').trim(), bbox: bb, lineIndex: i });
    }
    return lines;
  }
```

module の return に追加:

```javascript
    extractEdgeBBox: extractEdgeBBox,
    extractMultiLineTextBBoxes: extractMultiLineTextBBoxes,    // ← new
    hitTestTopmost: hitTestTopmost,
```

- [ ] **Step 4: Run tests to verify PASS**

- [ ] **Step 5: Commit**

```bash
git add src/core/overlay-builder.js tests/overlay-builder.test.js
git commit -m "feat(overlay-builder): extractMultiLineTextBBoxes (text/tspan modes)"
```

---

### Task 12: class.buildOverlay — Member rect 生成

**Files:**
- Modify: `src/modules/class.js` (buildOverlay 関数)
- Modify: `tests/class-overlay.test.js`

- [ ] **Step 1: Failing tests を追加**

```javascript
test('buildOverlay creates member rect per class member', () => {
  var dom = makeDom();
  var doc = dom.window.document;
  var svg = doc.createElementNS('http://www.w3.org/2000/svg', 'svg');
  var ent = doc.createElementNS('http://www.w3.org/2000/svg', 'g');
  ent.setAttribute('class', 'entity');
  ent.setAttribute('data-qualified-name', 'Foo');
  // Header text + 2 member texts
  ['Foo', '+ name : String', '+ login() : void'].forEach(function(label, idx) {
    var t = doc.createElementNS('http://www.w3.org/2000/svg', 'text');
    t.setAttribute('x', '10'); t.setAttribute('y', String(20 + idx * 14));
    t.textContent = label;
    ent.appendChild(t);
  });
  svg.appendChild(ent);

  var overlay = doc.createElementNS('http://www.w3.org/2000/svg', 'svg');
  var parsed = {
    elements: [{
      kind: 'class', id: 'Foo', label: 'Foo', stereotype: null, generics: null,
      line: 2, endLine: 4,
      members: [
        { kind: 'attribute', visibility: '+', name: 'name', type: 'String', static: false, abstract: false, line: 3 },
        { kind: 'method', visibility: '+', name: 'login', params: '', type: 'void', static: false, abstract: false, line: 4 },
      ],
    }],
    relations: [], groups: [], notes: [],
  };
  fns.plantumlClass.buildOverlay(svg, overlay, parsed);
  var memberRects = overlay.querySelectorAll('rect[data-type="member"]');
  assert.strictEqual(memberRects.length, 2);
  assert.strictEqual(memberRects[0].getAttribute('data-parent-id'), 'Foo');
  assert.strictEqual(memberRects[0].getAttribute('data-member-index'), '0');
  assert.strictEqual(memberRects[1].getAttribute('data-member-index'), '1');
});

test('buildOverlay member rect data-id encodes parent::index', () => {
  var dom = makeDom();
  var doc = dom.window.document;
  var svg = doc.createElementNS('http://www.w3.org/2000/svg', 'svg');
  var ent = doc.createElementNS('http://www.w3.org/2000/svg', 'g');
  ent.setAttribute('class', 'entity');
  ent.setAttribute('data-qualified-name', 'Foo');
  ['Foo', '+ x : int'].forEach(function(label, idx) {
    var t = doc.createElementNS('http://www.w3.org/2000/svg', 'text');
    t.setAttribute('x', '10'); t.setAttribute('y', String(20 + idx * 14));
    t.textContent = label;
    ent.appendChild(t);
  });
  svg.appendChild(ent);

  var overlay = doc.createElementNS('http://www.w3.org/2000/svg', 'svg');
  var parsed = {
    elements: [{
      kind: 'class', id: 'Foo', label: 'Foo', stereotype: null, generics: null,
      line: 2, endLine: 3,
      members: [{ kind: 'attribute', visibility: '+', name: 'x', type: 'int', static: false, abstract: false, line: 3 }],
    }],
    relations: [], groups: [], notes: [],
  };
  fns.plantumlClass.buildOverlay(svg, overlay, parsed);
  var r = overlay.querySelectorAll('rect[data-type="member"]')[0];
  assert.strictEqual(r.getAttribute('data-id'), 'Foo::__m_0');
});
```

- [ ] **Step 2: Run tests to verify FAIL**

- [ ] **Step 3: Implement member rect generation**

`src/modules/class.js` の `buildOverlay` 関数内、entity 処理ループの中で entity rect を生成した直後に追加:

```javascript
      // Member rects: one per class member, mapped to <text> lines after header
      if (entity.members && entity.members.length > 0) {
        var lines = OB.extractMultiLineTextBBoxes(g);
        // Header skip count: 1 (label) + (stereotype ? 1 : 0) + (generics ? 1 : 0)
        var headerSkip = 1;
        if (entity.stereotype) headerSkip++;
        if (entity.generics && entity.generics.length > 0) headerSkip++;
        var memberLines = lines.slice(headerSkip);
        var matchCount = Math.min(entity.members.length, memberLines.length);
        for (var mi = 0; mi < matchCount; mi++) {
          var ml = memberLines[mi];
          var mem = entity.members[mi];
          var bb = ml.bbox;
          // Expand width to entity rect width if available
          var rectW = bb.width || 80;
          OB.addRect(overlayEl, bb.x, bb.y, rectW, bb.height || 14, {
            'data-type': 'member',
            'data-id': entity.id + '::__m_' + mi,
            'data-parent-id': entity.id,
            'data-parent-kind': entity.kind,
            'data-member-index': String(mi),
            'data-member-kind': mem.kind,
            'data-line': String(mem.line),
          });
        }
        if (matchCount !== entity.members.length && typeof console !== 'undefined' && console.warn) {
          console.warn('[class.buildOverlay] member line mismatch for ' + entity.id +
            ': model=' + entity.members.length + ' svg=' + memberLines.length);
        }
      }
```

(`entity` は parse() の elements を iterate している loop 変数。`g` は対応する SVG `<g class="entity">` 要素。`OB` は overlayBuilder)

- [ ] **Step 4: Run tests to verify PASS**

- [ ] **Step 5: Commit**

```bash
git add src/modules/class.js tests/class-overlay.test.js
git commit -m "feat(class): buildOverlay member rect (data-type=member, parent-id, index)"
```

---

### Task 13: selection-router — shift+click 時の member coerce

**Files:**
- Modify: `src/core/selection-router.js`
- Test: 既存 router test がなければ test 不要 (Phase C E2E でカバー)

- [ ] **Step 1: Add coerce helper**

`src/core/selection-router.js` の `_isSameItem` の後に追加:

```javascript
  function _coerceMembersToParent(items) {
    return items.map(function(it) {
      if (it.type === 'member') {
        return { type: it.parentKind || 'class', id: it.parentId };
      }
      return it;
    });
  }
```

- [ ] **Step 2: Update bind() to use member-aware item creation**

`_itemFromTarget` を拡張:

```javascript
  function _itemFromTarget(target) {
    if (!target.getAttribute) return null;
    var type = target.getAttribute('data-type');
    var id = target.getAttribute('data-id');
    if (!type || !id) return null;
    var line = parseInt(target.getAttribute('data-line'), 10);
    var item = { type: type, id: id, line: isNaN(line) ? null : line };
    if (type === 'member') {
      item.parentId = target.getAttribute('data-parent-id');
      item.parentKind = target.getAttribute('data-parent-kind');
      item.memberIndex = parseInt(target.getAttribute('data-member-index'), 10);
      item.memberKind = target.getAttribute('data-member-kind');
    }
    return item;
  }
```

- [ ] **Step 3: Update bind() shift+click branch to coerce members**

`bind()` 内の shift+click handling を更新:

```javascript
      if (e.shiftKey) {
        var existing = current.filter(function(s) { return _isSameItem(s, item); });
        var nextSelection;
        if (existing.length > 0) {
          nextSelection = current.filter(function(s) { return !_isSameItem(s, item); });
        } else {
          nextSelection = current.concat([item]);
        }
        // Coerce members to parent for multi-select connect compatibility
        var coerced = _coerceMembersToParent(nextSelection);
        // Dedup by id after coerce
        var seen = {};
        var deduped = coerced.filter(function(s) {
          var k = s.type + ':' + s.id;
          if (seen[k]) return false;
          seen[k] = true;
          return true;
        });
        window.MA.selection.setSelected(deduped);
      } else {
```

- [ ] **Step 4: Manual smoke test (after Task 14)**

(member rect が SVG に存在するのは Task 14 後なので smoke test は連結後)

- [ ] **Step 5: Commit**

```bash
git add src/core/selection-router.js
git commit -m "feat(selection-router): coerce members to parent class on shift+click"
```

---

### Task 14: renderProps — type=member 分岐 + auto-expand inline edit

**Files:**
- Modify: `src/modules/class.js`

- [ ] **Step 1: Update renderProps to handle member selection**

`renderProps` 関数を更新 (note 判定の後、renderByDispatch の前):

```javascript
  function renderProps(selData, parsedData, propsEl, ctx) {
    if (!propsEl) return;
    if (selData && selData.length === 1 && selData[0].type === 'note') {
      var note = null;
      for (var i = 0; i < (parsedData.notes || []).length; i++) {
        if (parsedData.notes[i].id === selData[0].id) { note = parsedData.notes[i]; break; }
      }
      if (note) { _renderNoteEdit(note, parsedData, propsEl, ctx); return; }
    }
    if (selData && selData.length === 1 && selData[0].type === 'member') {
      var sel = selData[0];
      var parent = null;
      for (var j = 0; j < parsedData.elements.length; j++) {
        if (parsedData.elements[j].id === sel.parentId) { parent = parsedData.elements[j]; break; }
      }
      if (parent) {
        _renderElementEdit(parent, parsedData, propsEl, ctx, { focusMemberIndex: sel.memberIndex });
        return;
      }
    }
    window.MA.propsRenderer.renderByDispatch(selData, parsedData, propsEl, {
      onNoSelection: function(p, e) { _renderNoSelection(p, e, ctx); },
      onElement: function(elt, p, e) { _renderElementEdit(elt, p, e, ctx); },
      onRelation: function(rel, p, e) { _renderRelationEdit(rel, p, e, ctx); },
      onGroup: function(grp, p, e) { _renderGroupReadOnly(grp, p, e, ctx); },
      onMultiSelectConnect: function(s, p, e) { _renderMultiSelectConnect(s, p, e, ctx); },
      onMultiSelect: function(s, p, e) { _renderMultiSelect(s, p, e); },
    });
  }
```

- [ ] **Step 2: Update `_renderElementEdit` signature to accept opts**

`_renderElementEdit` の関数定義を:

```javascript
  function _renderElementEdit(element, parsedData, propsEl, ctx, opts) {
    if (element.kind === 'enum') return _renderEnumEdit(element, parsedData, propsEl, ctx, opts);
    var P = window.MA.properties;
    var focusIdx = opts && typeof opts.focusMemberIndex === 'number' ? opts.focusMemberIndex : -1;
    // ... 以下既存
```

`_renderEnumEdit` も同様 opts 受け取り。

- [ ] **Step 3: Render member rows with selected highlight + auto-expand**

`_renderElementEdit` 内 member 描画ロジックを置き換える。既存実装は `attrs` / `methods` を別 section で描画し、削除は `bindAllByClass(propsEl, 'cl-mem-del', ...)` で一括 bind している (Line 717-794 付近)。これを以下の **uniform `element.members.forEach` ループ** に置換:

```javascript
    // Members (uniform loop across attributes + methods)
    if (element.members && element.members.length > 0) {
      html += '<div style="border-top:1px solid var(--border);padding-top:6px;margin-top:6px;">' +
              '<div style="font-size:10px;color:var(--accent);font-weight:bold;margin-bottom:4px;">Members</div>';
      element.members.forEach(function(m, mi) {
        var sel = mi === focusIdx;
        var rowCls = sel ? 'cl-member-row cl-member-selected' : 'cl-member-row';
        var rowStyle = sel ? 'background:var(--accent-bg, rgba(0,128,255,0.15));padding:4px;border-radius:3px;' : 'padding:2px;';
        var preview = (m.visibility || '') + ' ' + m.name +
                      (m.kind === 'method' ? '(' + (m.params || '') + ')' : '') +
                      (m.type ? ' : ' + m.type : '');
        html += '<div class="' + rowCls + '" data-member-idx="' + mi + '" style="' + rowStyle + 'font-size:11px;margin-bottom:2px;">' +
                  preview +
                  ' <button id="cl-mem-up-' + mi + '" data-line="' + m.line + '">↑</button>' +
                  ' <button id="cl-mem-down-' + mi + '" data-line="' + m.line + '">↓</button>' +
                  ' <button id="cl-mem-del-' + mi + '" data-line="' + m.line + '">✕</button>';
        if (sel) {
          // Inline edit fields (only for selected row)
          html += '<div style="margin-top:4px;padding:4px;background:var(--bg);border:1px solid var(--border);">' +
                    P.selectFieldHtml('Visibility', 'cl-mem-vis-' + mi, [
                      { value: '+', label: '+', selected: m.visibility === '+' },
                      { value: '-', label: '-', selected: m.visibility === '-' },
                      { value: '#', label: '#', selected: m.visibility === '#' },
                      { value: '~', label: '~', selected: m.visibility === '~' },
                      { value: '',  label: '(none)', selected: !m.visibility },
                    ]) +
                    P.fieldHtml('Name', 'cl-mem-name-' + mi, m.name) +
                    P.fieldHtml('Type', 'cl-mem-type-' + mi, m.type || '') +
                    (m.kind === 'method' ? P.fieldHtml('Params', 'cl-mem-params-' + mi, m.params || '') : '') +
                    '<div style="margin-top:4px;">' +
                      '<label><input type="checkbox" id="cl-mem-static-' + mi + '"' + (m.static ? ' checked' : '') + '> static</label>' +
                      (m.kind === 'method' ? ' <label><input type="checkbox" id="cl-mem-abstract-' + mi + '"' + (m.abstract ? ' checked' : '') + '> abstract</label>' : '') +
                    '</div>' +
                    P.primaryButtonHtml('cl-mem-update-' + mi, '更新') +
                  '</div>';
        }
        html += '</div>';
      });
      html += '</div>';
    }
```

**Important**: 上記置換時に既存の `bindAllByClass(propsEl, 'cl-mem-del', ...)` 呼び出し (Line 789-794 付近) も削除する。新ループの per-mi `cl-mem-del-N` handler が代替する。`cl-add-attr` / `cl-add-method` ボタン + form (Line 795-848 付近) はそのまま残す (新規 member 追加 expander として機能継続)。

- [ ] **Step 4: Wire member row click handlers + update handler**

`propsEl.innerHTML = html;` の後に追加:

```javascript
    element.members.forEach(function(m, mi) {
      // Row click → switch selection to that member
      var row = propsEl.querySelector('.cl-member-row[data-member-idx="' + mi + '"]');
      if (row && mi !== focusIdx) {
        row.style.cursor = 'pointer';
        row.addEventListener('click', function() {
          ctx.setSelectedHighlight([{
            type: 'member',
            id: element.id + '::__m_' + mi,
            parentId: element.id,
            parentKind: element.kind,
            memberIndex: mi,
            memberKind: m.kind,
            line: m.line,
          }]);
          ctx.onUpdate();
        });
      }
      // Update / move / delete handlers
      P.bindEvent('cl-mem-up-' + mi, 'click', function(e) {
        e.stopPropagation();
        window.MA.history.pushHistory();
        ctx.setMmdText(moveLineUp(ctx.getMmdText(), m.line));
        ctx.onUpdate();
      });
      P.bindEvent('cl-mem-down-' + mi, 'click', function(e) {
        e.stopPropagation();
        window.MA.history.pushHistory();
        ctx.setMmdText(moveLineDown(ctx.getMmdText(), m.line));
        ctx.onUpdate();
      });
      P.bindEvent('cl-mem-del-' + mi, 'click', function(e) {
        e.stopPropagation();
        window.MA.history.pushHistory();
        ctx.setMmdText(deleteMember(ctx.getMmdText(), m.line));
        ctx.setSelectedHighlight([]);
        ctx.onUpdate();
      });
      if (mi === focusIdx) {
        P.bindEvent('cl-mem-update-' + mi, 'click', function() {
          var vis = document.getElementById('cl-mem-vis-' + mi).value || null;
          var name = document.getElementById('cl-mem-name-' + mi).value;
          var typ = document.getElementById('cl-mem-type-' + mi).value;
          var stat = !!document.getElementById('cl-mem-static-' + mi).checked;
          window.MA.history.pushHistory();
          if (m.kind === 'attribute') {
            ctx.setMmdText(updateAttribute(ctx.getMmdText(), m.line, vis, name, typ, stat));
          } else if (m.kind === 'method') {
            var pms = document.getElementById('cl-mem-params-' + mi).value;
            var abs = !!document.getElementById('cl-mem-abstract-' + mi).checked;
            ctx.setMmdText(updateMethod(ctx.getMmdText(), m.line, vis, name, pms, typ, stat, abs));
          }
          ctx.onUpdate();
        });
        // Auto-scroll selected row into view
        setTimeout(function() {
          var r = propsEl.querySelector('.cl-member-selected');
          if (r && r.scrollIntoView) r.scrollIntoView({ block: 'nearest' });
        }, 0);
      }
    });
```

- [ ] **Step 5: Manual smoke test**

`python server.py` 起動 → Class diagram template を render → `User` クラスを overlay click でクラス選択を確認 → SVG 上で member 行 (例 `+ id : int`) をクリック → property panel で User 全体表示 + 該当 member 行 highlight + inline edit fields 展開 + auto-scroll を確認。値変更 → 更新 → editor 反映を確認。Shift+click で別クラス member → 親クラスに coerce → multi-select connect form が出ることを確認。

- [ ] **Step 6: Commit**

```bash
git add src/modules/class.js
git commit -m "feat(class): renderProps member-type branch + auto-expand inline edit + auto-scroll"
```

---

## Phase C: E2E + Docs (Tasks 15-16)

### Task 15: E2E spec — class-v0.6.1

**Files:**
- Create: `tests/e2e/class-v0.6.1.spec.js`

- [ ] **Step 1: Write E2E spec**

```javascript
// @ts-check
const { test, expect } = require('@playwright/test');
const { gotoApp, getEditorText } = require('./helpers');

test.describe('Class v0.6.1 polish', () => {
  test.describe('Note on class', () => {
    test('tail-add note kind appends note left of class', async ({ page }) => {
      await gotoApp(page);
      await page.locator('#diagram-type').selectOption('plantuml-class');
      await page.waitForTimeout(500);
      await page.locator('#cl-tail-kind').selectOption('note');
      await page.waitForTimeout(200);
      await page.locator('#cl-tail-ntarget').selectOption('User');
      await page.locator('#cl-tail-npos').selectOption('left');
      await page.locator('#cl-tail-ntext').fill('hello note');
      await page.locator('#cl-tail-add').click();
      await page.waitForTimeout(300);
      var t = await getEditorText(page);
      expect(t).toContain('note left of User : hello note');
    });

    test('multi-line note via class panel emits end note block', async ({ page }) => {
      await gotoApp(page);
      await page.locator('#diagram-type').selectOption('plantuml-class');
      await page.waitForTimeout(2500);
      var rect = page.locator('#overlay-layer rect[data-type="class"]').first();
      var c = await rect.count();
      if (c === 0) test.skip();
      await rect.click();
      await page.waitForTimeout(300);
      await page.locator('#cl-add-note-btn').click();
      await page.waitForTimeout(200);
      await page.locator('#cl-new-npos').selectOption('right');
      await page.locator('#cl-new-ntext').fill('first\nsecond');
      await page.locator('#cl-new-nadd').click();
      await page.waitForTimeout(300);
      var t = await getEditorText(page);
      expect(t).toContain('note right of');
      expect(t).toContain('end note');
      expect(t).toContain('first');
      expect(t).toContain('second');
    });

    test('deleting class removes its notes (cascade)', async ({ page }) => {
      await gotoApp(page);
      await page.locator('#diagram-type').selectOption('plantuml-class');
      await page.waitForTimeout(500);
      // Add a note first
      await page.locator('#cl-tail-kind').selectOption('note');
      await page.waitForTimeout(200);
      await page.locator('#cl-tail-ntarget').selectOption('User');
      await page.locator('#cl-tail-ntext').fill('cascade me');
      await page.locator('#cl-tail-add').click();
      await page.waitForTimeout(2500);
      // Click User class on overlay
      var rect = page.locator('#overlay-layer rect[data-type="class"][data-id="User"]').first();
      var c = await rect.count();
      if (c === 0) test.skip();
      await rect.click();
      await page.waitForTimeout(300);
      await page.locator('#cl-element-delete').click();
      await page.waitForTimeout(300);
      var t = await getEditorText(page);
      expect(t).not.toContain('class User');
      expect(t).not.toContain('note left of User');
      expect(t).not.toContain('cascade me');
    });
  });

  test.describe('Member individual click', () => {
    test('clicking member rect selects member and expands inline edit', async ({ page }) => {
      await gotoApp(page);
      await page.locator('#diagram-type').selectOption('plantuml-class');
      await page.waitForTimeout(2500);
      var memRect = page.locator('#overlay-layer rect[data-type="member"]').first();
      var mc = await memRect.count();
      if (mc === 0) test.skip();
      await memRect.click();
      await page.waitForTimeout(300);
      // Inline edit fields should be present
      var nameField = await page.locator('input[id^="cl-mem-name-"]').count();
      expect(nameField).toBeGreaterThan(0);
    });

    test('shift+click member coerces to parent class for multi-select connect', async ({ page }) => {
      await gotoApp(page);
      await page.locator('#diagram-type').selectOption('plantuml-class');
      await page.waitForTimeout(2500);
      var classRect = page.locator('#overlay-layer rect[data-type="class"]').first();
      var iface = page.locator('#overlay-layer rect[data-type="interface"]').first();
      if ((await classRect.count()) === 0 || (await iface.count()) === 0) test.skip();
      await classRect.click();
      // Try to find a member rect inside the interface (or a 2nd class)
      var anyMember = page.locator('#overlay-layer rect[data-type="member"]').first();
      var mc = await anyMember.count();
      if (mc === 0) {
        // Fallback: shift+click another class
        await iface.click({ modifiers: ['Shift'] });
      } else {
        await anyMember.click({ modifiers: ['Shift'] });
      }
      await page.waitForTimeout(300);
      var connKind = await page.locator('#cl-conn-kind').count();
      expect(connKind).toBeGreaterThan(0);
    });

    test('console error count is 0 during member + note interactions', async ({ page }) => {
      var errors = [];
      page.on('console', function(msg) { if (msg.type() === 'error') errors.push(msg.text()); });
      await gotoApp(page);
      await page.locator('#diagram-type').selectOption('plantuml-class');
      await page.waitForTimeout(2500);
      var c = page.locator('#overlay-layer rect[data-type="class"]').first();
      if ((await c.count()) > 0) {
        await c.click();
        await page.waitForTimeout(200);
      }
      var m = page.locator('#overlay-layer rect[data-type="member"]').first();
      if ((await m.count()) > 0) {
        await m.click();
        await page.waitForTimeout(200);
      }
      var jsErrors = errors.filter(function(e) { return e.indexOf('favicon') < 0; });
      expect(jsErrors).toHaveLength(0);
    });
  });
});
```

- [ ] **Step 2: Run E2E**

Run: `npx playwright test tests/e2e/class-v0.6.1.spec.js`
Expected: 6 tests pass (online PlantUML 経由のため 2500ms wait が必要)

- [ ] **Step 3: Commit**

```bash
git add tests/e2e/class-v0.6.1.spec.js
git commit -m "test(class): E2E coverage for v0.6.1 (member click + note)"
```

---

### Task 16: README + CHANGELOG

**Files:**
- Modify: `README.md`
- Modify: `CHANGELOG.md`

- [ ] **Step 1: CHANGELOG entry**

`CHANGELOG.md` の `## [0.6.0]` の上に追加:

```markdown
## [0.6.1] - 2026-04-27

### Added
- **Member 個別 SVG クリック選択** — class / interface / abstract / enum の attribute / method / enum-value 行を SVG で直接 click → property panel が自動 expand inline edit + auto-scroll
- **Note on class** — 1行 + 複数行 directional note (`note (left|right|top|bottom) of NAME`) の parse / formatter / add / update / delete + class 削除時の cascade
- `core/overlay-builder.js` `extractMultiLineTextBBoxes(g, opts)` API 追加 (Activity v0.7.0 と共有予定)
- ADR-108: Note canonical DSL form
- E2E: `class-v0.6.1.spec.js` (6 tests, member click + note)

### Changed
- `src/core/selection-router.js` shift+click 時に `member` type の selection を `parentClass` に coerce + dedup (multi-select connect 互換性のため)
- `src/modules/class.js` `_renderElementEdit` に opts.focusMemberIndex 受付追加 (member 選択時の auto-expand 用)

### Notes
- v0.7.0 へ繰越: 内部クラス、floating note (`note "..." as N`) + link、note on relation、member の drag-to-reorder
- 設計詳細: `docs/superpowers/specs/2026-04-27-class-v0.6.1-design.md`
- 実装計画: `docs/superpowers/plans/2026-04-27-class-v0.6.1.md`
```

- [ ] **Step 2: README v0.6.1 section**

`README.md` の v0.6.0 セクションの直後に追加:

```markdown
### v0.6.1 — Class polish (Member click + Note on class)

- **Member 個別 click**: SVG 上の attribute / method / enum-value 行を直接クリック → 該当 member の inline edit が auto-expand
- **Note on class**: 1 行 / 複数行 directional note を property panel から作成・編集・削除。class 削除時に紐付く note も自動削除
- **Activity v0.7.0 へ向けた API**: `extractMultiLineTextBBoxes` を `core/overlay-builder` に汎化、複数行 text の per-line bbox 取得を共通化
```

- [ ] **Step 3: Commit**

```bash
git add README.md CHANGELOG.md
git commit -m "docs: v0.6.1 README + CHANGELOG"
```

---

## Final verification

- [ ] **Step 1: Full unit test suite**

Run: `node tests/run-tests.js`
Expected: 全 PASS (374 + ~33 新規 ≈ 407 tests)

- [ ] **Step 2: Full E2E**

Run: `npx playwright test`
Expected: 91 既存 + 6 新規 = 97 tests, 全 PASS

- [ ] **Step 3: Manual smoke test**

`python server.py` 起動 → Class 図を template から render →
1. Class クリック → property panel に notes セクション + Note expander 表示
2. Member 行クリック → inline edit auto-expand + auto-scroll
3. Tail-add note kind → editor に note 追加
4. Class 削除 → 紐付く note も同時削除
5. Shift+click で member + class → connect form 表示
6. Console errors 0 件

- [ ] **Step 4: Visual sweep (optional, user-trigger)**

Evaluator subagent dispatch — 6 cells (member-α / member-γ / note-α / note-γ-form / note-γ-overlay / cascade-γ)

---

## PR ハンドオフ

PR 作成は user 承認が必要 (CLAUDE.md 規約)。本 plan 完了後の PR draft 例:

```markdown
## Summary
v0.6.1 Class diagram polish: Member 個別 SVG クリック選択 + Note on class (1行+複数行 directional) を追加。`extractMultiLineTextBBoxes` API を `core/overlay-builder` に汎化 (Activity v0.7.0 と共有予定)。

## Test plan
- [x] Unit tests: 全 ~407 passed (374 → 407, +33)
- [x] E2E: class-v0.6.1.spec.js 6 tests passed
- [x] 既存 E2E regression: 91 全 passed
- [x] Manual smoke test: member click + note CRUD + cascade delete + console error 0

🤖 Generated with [Claude Code](https://claude.com/claude-code)
```
